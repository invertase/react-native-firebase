# Vendored Homebrew formulae for Tart local repro only.

Copies of `.github/homebrew-rnfb/Formula/` — keep in sync when CI pins change
(see okf-bundle/ci-workflows/ios.md#pinned-homebrew-utilities).

Do not reference this tap from `.github/workflows/`.
