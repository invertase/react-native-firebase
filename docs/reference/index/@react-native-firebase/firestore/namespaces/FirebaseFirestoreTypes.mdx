[**React Native Firebase**](../../../../README.mdx)

***

[React Native Firebase](../../../../packages.mdx) / [@react-native-firebase/firestore](../../../../@react-native-firebase/firestore/README.mdx) / [index](../index.mdx) / FirebaseFirestoreTypes

# FirebaseFirestoreTypes

Firebase Cloud Firestore package for React Native.

#### Example: Access the firebase export from the `firestore` package:

```js
import { firebase } from '@react-native-firebase/firestore';

// firebase.firestore().X
```

#### Example: Using the default export from the `firestore` package:

```js
import firestore from '@react-native-firebase/firestore';

// firestore().X
```

#### Example: Using the default export from the `app` package:

```js
import firebase from '@react-native-firebase/app';
import '@react-native-firebase/firestore';

// firebase.firestore().X
```

## Firebase

firestore

## Classes

### AggregateField

Defined in: [types/namespaced.ts:984](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L984)

Represents an aggregation that can be performed by Firestore.

#### Type Parameters

##### _T

`_T`

#### Constructors

##### Constructor

> **new AggregateField**\<`_T`\>(): [`AggregateField`](#aggregatefield)\<`_T`\>

###### Returns

[`AggregateField`](#aggregatefield)\<`_T`\>

#### Properties

##### type

> **type**: `string` = `'AggregateField'`

Defined in: [types/namespaced.ts:986](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L986)

A type string to uniquely identify instances of this class.

***

### Blob

Defined in: [types/namespaced.ts:107](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L107)

An immutable object representing an array of bytes.

#### Constructors

##### Constructor

> **new Blob**(): [`Blob`](#blob)

###### Returns

[`Blob`](#blob)

#### Methods

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:127](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L127)

Returns true if this `Blob` is equal to the provided one.

###### Parameters

###### other

[`Blob`](#blob)

The `Blob` to compare against.

###### Returns

`boolean`

##### toBase64()

> **toBase64**(): `string`

Defined in: [types/namespaced.ts:132](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L132)

Returns the bytes of a Blob as a Base64-encoded string.

###### Returns

`string`

##### toUint8Array()

> **toUint8Array**(): `Uint8Array`

Defined in: [types/namespaced.ts:137](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L137)

Returns the bytes of a Blob in a new Uint8Array.

###### Returns

`Uint8Array`

##### fromBase64String()

> `static` **fromBase64String**(`base64`): [`Blob`](#blob)

Defined in: [types/namespaced.ts:113](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L113)

Creates a new Blob from the given Base64 string, converting it to bytes.

###### Parameters

###### base64

`string`

The Base64 string used to create the Blob object.

###### Returns

[`Blob`](#blob)

##### fromUint8Array()

> `static` **fromUint8Array**(`array`): [`Blob`](#blob)

Defined in: [types/namespaced.ts:120](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L120)

Creates a new Blob from the given Uint8Array.

###### Parameters

###### array

`Uint8Array`

The Uint8Array used to create the Blob object.

###### Returns

[`Blob`](#blob)

***

### CollectionReference

Defined in: [types/namespaced.ts:151](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L151)

A `CollectionReference` object can be used for adding documents, getting document references, and querying for
documents (using the methods inherited from `Query`).

#### Extends

- [`Query`](#query)\<`AppModelType`, `DbModelType`\>

#### Type Parameters

##### AppModelType

`AppModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

##### DbModelType

`DbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Constructors

##### Constructor

> **new CollectionReference**\<`AppModelType`, `DbModelType`\>(): [`CollectionReference`](#collectionreference)\<`AppModelType`, `DbModelType`\>

###### Returns

[`CollectionReference`](#collectionreference)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`constructor`](#constructor-8)

#### Methods

##### add()

> **add**(`data`): `Promise`\<[`DocumentReference`](#documentreference)\<`AppModelType`, `DbModelType`\>\>

Defined in: [types/namespaced.ts:185](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L185)

Add a new document to this collection with the specified data, assigning it a document ID automatically.

#### Example

```js
const documentRef = await firebase.firestore().collection('users').add({
  name: 'Ada Lovelace',
  age: 30,
});
```

###### Parameters

###### data

`AppModelType`

An Object containing the data for the new document.

###### Returns

`Promise`\<[`DocumentReference`](#documentreference)\<`AppModelType`, `DbModelType`\>\>

##### count()

> **count**(): [`AggregateQuery`](#aggregatequery)\<\{ `count`: [`AggregateField`](#aggregatefield)\<`number`\>; \}\>

Defined in: [types/namespaced.ts:1086](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1086)

Calculates the number of documents in the result set of the given query, without actually downloading
the documents.

Using this function to count the documents is efficient because only the final count, not the
documents' data, is downloaded. This function can even count the documents if the result set
would be prohibitively large to download entirely (e.g. thousands of documents).

The result received from the server is presented, unaltered, without considering any local state.
That is, documents in the local cache are not taken into consideration, neither are local
modifications not yet synchronized with the server. Previously-downloaded results, if any,
 are not used: every request using this source necessarily involves a round trip to the server.

###### Returns

[`AggregateQuery`](#aggregatequery)\<\{ `count`: [`AggregateField`](#aggregatefield)\<`number`\>; \}\>

###### Inherited from

[`Query`](#query).[`count`](#count-1)

##### countFromServer()

> **countFromServer**(): [`AggregateQuery`](#aggregatequery)\<\{ `count`: [`AggregateField`](#aggregatefield)\<`number`\>; \}\>

Defined in: [types/namespaced.ts:1091](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1091)

Same as count()

###### Returns

[`AggregateQuery`](#aggregatequery)\<\{ `count`: [`AggregateField`](#aggregatefield)\<`number`\>; \}\>

###### Inherited from

[`Query`](#query).[`countFromServer`](#countfromserver-1)

##### doc()

> **doc**(`documentPath?`): [`DocumentReference`](#documentreference)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:202](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L202)

Get a DocumentReference for the document within the collection at the specified path. If no
path is specified, an automatically-generated unique ID will be used for the returned DocumentReference.

#### Example

```js
await firebase.firestore().collection('users').doc('alovelace').set({
  name: 'Ada Lovelace',
  age: 30,
});
```

###### Parameters

###### documentPath?

`string`

A slash-separated path to a document.

###### Returns

[`DocumentReference`](#documentreference)\<`AppModelType`, `DbModelType`\>

##### endAt()

###### Call Signature

> **endAt**(`snapshot`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1114](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1114)

Creates and returns a new Query that ends at the provided document (inclusive). The end
position is relative to the order of the query. The document must contain all of the
fields provided in the orderBy of this query.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

// Get all users up to a specific user in order of age
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .endAt(user);
```

> Cursor snapshot queries have limitations. Please see [Query limitations](/query-limitations) for more information.

###### Parameters

###### snapshot

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The snapshot of the document to end at.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`endAt`](#endat-1)

###### Call Signature

> **endAt**(...`fieldValues`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1132](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1132)

Creates and returns a new Query that ends at the provided fields relative to the order of the query.
The order of the field values must match the order of the order by clauses of the query.

#### Example

```js
// Get all users who's age is 30 or less
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .endAt(30);
```

###### Parameters

###### fieldValues

...`any`[]

The field values to end this query at, in order of the query's order by.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`endAt`](#endat-1)

##### endBefore()

###### Call Signature

> **endBefore**(`snapshot`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1155](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1155)

Creates and returns a new Query that ends before the provided document (exclusive). The end
position is relative to the order of the query. The document must contain all of the fields
provided in the orderBy of this query.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

// Get all users up to, but not including, a specific user in order of age
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .endBefore(user);
```

> Cursor snapshot queries have limitations. Please see [Query limitations](/query-limitations) for more information.

###### Parameters

###### snapshot

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The snapshot of the document to end before.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`endBefore`](#endbefore-1)

###### Call Signature

> **endBefore**(...`fieldValues`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1175](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1175)

Creates and returns a new Query that ends before the provided fields relative to the order of
the query. The order of the field values must match the order of the order by clauses of the query.

#### Example

```js
// Get all users who's age is 29 or less
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .endBefore(30);
```

###### Parameters

###### fieldValues

...`any`[]

The field values to end this query before, in order of the query's order by.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`endBefore`](#endbefore-1)

##### get()

> **get**(`options?`): `Promise`\<[`QuerySnapshot`](#querysnapshot)\<`AppModelType`, `DbModelType`\>\>

Defined in: [types/namespaced.ts:1197](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1197)

Executes the query and returns the results as a QuerySnapshot.

Note: By default, get() attempts to provide up-to-date data when possible by waiting for data from the server,
but it may return cached data or fail if you are offline and the server cannot be reached. This behavior can be
altered via the `GetOptions` parameter.

#### Example

```js
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .get({
    source: 'server',
  });
```

###### Parameters

###### options?

[`GetOptions`](#getoptions)

An object to configure the get behavior.

###### Returns

`Promise`\<[`QuerySnapshot`](#querysnapshot)\<`AppModelType`, `DbModelType`\>\>

###### Inherited from

[`Query`](#query).[`get`](#get-2)

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:1219](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1219)

Returns true if this Query is equal to the provided one.

#### Example

```js
const query = firebase.firestore()
  .collection('users')
  .orderBy('age');

// false
query.isEqual(
  firebase.firestore()
    .collection('users')
    .orderBy('name')
);
```

###### Parameters

###### other

[`Query`](#query)

The `Query` to compare against.

###### Returns

`boolean`

###### Inherited from

[`Query`](#query).[`isEqual`](#isequal-6)

##### limit()

> **limit**(`limit`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1237](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1237)

Creates and returns a new Query where the results are limited to the specified number of documents.

#### Example

```js
// Get 10 users in order of age
const querySnapshot = firebase.firestore()
  .collection('users')
  .orderBy('age')
  .limit(10)
  .get();
```

###### Parameters

###### limit

`number`

The maximum number of items to return.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`limit`](#limit-1)

##### limitToLast()

> **limitToLast**(`limitToLast`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1257](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1257)

Creates and returns a new Query where the results are limited to the specified number of documents
starting from the last document. The order is dependent on the second parameter for the `orderBy`
method. If `desc` is used, the order is reversed. `orderBy` method call is required when calling `limitToLast`.

#### Example

```js
// Get the last 10 users in reverse order of age
const querySnapshot = firebase.firestore()
  .collection('users')
  .orderBy('age', 'desc')
  .limitToLast(10)
  .get();
```

###### Parameters

###### limitToLast

`number`

The maximum number of items to return.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`limitToLast`](#limittolast-1)

##### onSnapshot()

###### Call Signature

> **onSnapshot**(`observer`): () => `void`

Defined in: [types/namespaced.ts:1280](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1280)

Attaches a listener for `QuerySnapshot` events.

> Although an `onCompletion` callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().collection('users')
  .onSnapshot({
    error: (e) => console.error(e),
    next: (querySnapshot) => {},
  });

unsubscribe();
```

###### Parameters

###### observer

A single object containing `next` and `error` callbacks.

###### complete?

() => `void`

###### error?

(`error`) => `void`

###### next?

(`snapshot`) => `void`

###### Returns

() => `void`

###### Inherited from

[`Query`](#query).[`onSnapshot`](#onsnapshot-2)

###### Call Signature

> **onSnapshot**(`options`, `observer`): () => `void`

Defined in: [types/namespaced.ts:1310](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1310)

Attaches a listener for `QuerySnapshot` events with snapshot listener options.

> Although an `onCompletion` callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().collection('users')
  .onSnapshot({
    includeMetadataChanges: true,
  }, {
    error: (e) => console.error(e),
    next: (querySnapshot) => {},
  });

unsubscribe();
```

###### Parameters

###### options

[`SnapshotListenOptions`](#snapshotlistenoptions)

Options controlling the listen behavior.

###### observer

A single object containing `next` and `error` callbacks.

###### complete?

() => `void`

###### error?

(`error`) => `void`

###### next?

(`snapshot`) => `void`

###### Returns

() => `void`

###### Inherited from

[`Query`](#query).[`onSnapshot`](#onsnapshot-2)

###### Call Signature

> **onSnapshot**(`onNext`, `onError?`, `onCompletion?`): () => `void`

Defined in: [types/namespaced.ts:1341](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1341)

Attaches a listener for `QuerySnapshot` events.

> Although an `onCompletion` callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().collection('users')
  .onSnapshot(
    (querySnapshot) => {}, // onNext
    (error) => console.error(error), // onError
  );

unsubscribe();
```

###### Parameters

###### onNext

(`snapshot`) => `void`

A callback to be called every time a new `QuerySnapshot` is available.

###### onError?

(`error`) => `void`

A callback to be called if the listen fails or is cancelled. No further callbacks will occur.

###### onCompletion?

() => `void`

An optional function which will never be called.

###### Returns

() => `void`

###### Inherited from

[`Query`](#query).[`onSnapshot`](#onsnapshot-2)

###### Call Signature

> **onSnapshot**(`options`, `onNext`, `onError?`, `onCompletion?`): () => `void`

Defined in: [types/namespaced.ts:1371](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1371)

Attaches a listener for `QuerySnapshot` events with snapshot listener options.

NOTE: Although an onCompletion callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().collection('users')
  .onSnapshot(
    { includeMetadataChanges: true }, // SnapshotListenerOptions
    (querySnapshot) => {}, // onNext
    (error) => console.error(error), // onError
  );

unsubscribe();
```

###### Parameters

###### options

[`SnapshotListenOptions`](#snapshotlistenoptions)

Options controlling the listen behavior.

###### onNext

(`snapshot`) => `void`

A callback to be called every time a new `QuerySnapshot` is available.

###### onError?

(`error`) => `void`

A callback to be called if the listen fails or is cancelled. No further callbacks will occur.

###### onCompletion?

() => `void`

An optional function which will never be called.

###### Returns

() => `void`

###### Inherited from

[`Query`](#query).[`onSnapshot`](#onsnapshot-2)

##### orderBy()

> **orderBy**(`fieldPath`, `directionStr?`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1396](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1396)

Creates and returns a new Query that's additionally sorted by the specified field, optionally in descending order instead of ascending.

* #### Example

#### Example

```js
// Get users in order of age, descending
const querySnapshot = firebase.firestore()
  .collection('users')
  .orderBy('age', 'desc')
  .get();
```

###### Parameters

###### fieldPath

`string` \| [`FieldPath`](#fieldpath)

The field to sort by. Either a string or FieldPath instance.

###### directionStr?

`"desc"` \| `"asc"`

Optional direction to sort by (`asc` or `desc`). If not specified, order will be ascending.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`orderBy`](#orderby-1)

##### startAfter()

###### Call Signature

> **startAfter**(`snapshot`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1423](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1423)

Creates and returns a new Query that starts after the provided document (exclusive). The start
position is relative to the order of the query. The document must contain all of the fields
provided in the orderBy of this query.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

// Get all users up to, but not including, a specific user in order of age
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .startAfter(user)
  .get();
```

> Cursor snapshot queries have limitations. Please see [Query limitations](/query-limitations) for more information.

###### Parameters

###### snapshot

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The snapshot of the document to start after.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`startAfter`](#startafter-1)

###### Call Signature

> **startAfter**(...`fieldValues`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1444](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1444)

Creates and returns a new Query that starts after the provided fields relative to the order of
the query. The order of the field values must match the order of the order by clauses of the query.

#### Example

```js
// Get all users who's age is above 30
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .startAfter(30)
  .get();
```

###### Parameters

###### fieldValues

...`any`[]

The field values to start this query after, in order of the query's order by.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`startAfter`](#startafter-1)

##### startAt()

###### Call Signature

> **startAt**(`snapshot`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1468](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1468)

Creates and returns a new Query that starts at the provided document (inclusive). The start
position is relative to the order of the query. The document must contain all of the
fields provided in the orderBy of this query.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

// Get all users up to a specific user in order of age
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .startAt(user)
  .get();
```

> Cursor snapshot queries have limitations. Please see [Query limitations](/query-limitations) for more information.

###### Parameters

###### snapshot

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The snapshot of the document to start at.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`startAt`](#startat-1)

###### Call Signature

> **startAt**(...`fieldValues`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1489](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1489)

Creates and returns a new Query that starts at the provided fields relative to the order of the query.
The order of the field values must match the order of the order by clauses of the query.

#### Example

```js
// Get all users who's age is 30 or above
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .startAt(30)
  .get();
```

###### Parameters

###### fieldValues

...`any`[]

The field values to start this query at, in order of the query's order by.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`startAt`](#startat-1)

##### where()

###### Call Signature

> **where**(`fieldPath`, `opStr`, `value`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1509](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1509)

Creates and returns a new Query with the additional filter that documents must contain the specified field and
the value should satisfy the relation constraint provided.

#### Example

```js
// Get all users who's age is 30 or above
const querySnapshot = await firebase.firestore()
  .collection('users')
  .where('age', '>=', 30);
  .get();
```

###### Parameters

###### fieldPath

`string` \| [`FieldPath`](#fieldpath)

The path to compare.

###### opStr

[`WhereFilterOp`](#wherefilterop)

The operation string (e.g "<", "<=", "==", ">", ">=", "!=", "array-contains", "array-contains-any", "in", "not-in").

###### value

`any`

The comparison value.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`where`](#where-1)

###### Call Signature

> **where**(`filter`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1531](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1531)

Creates and returns a new Query with the additional filter that documents must contain the specified field and
the value should satisfy the relation constraint provided.

#### Example

```js
// Get all users who's age is 30 or above
const querySnapshot = await firebase.firestore()
  .collection('users')
  .where(Filter('age', '>=', 30));
  .get();
```

###### Parameters

###### filter

[`QueryFilterConstraint`](#queryfilterconstraint)

The filter to apply to the query.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Inherited from

[`Query`](#query).[`where`](#where-1)

##### withConverter()

###### Call Signature

> **withConverter**(`converter`): [`CollectionReference`](#collectionreference)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

Defined in: [types/namespaced.ts:217](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L217)

Applies a custom data converter to this CollectionReference, allowing you
to use your own custom model objects with Firestore. When you call add()
on the returned CollectionReference instance, the provided converter will
convert between Firestore data and your custom type U.

Passing in `null` as the converter parameter removes the current
converter.

###### Parameters

###### converter

`null`

Converts objects to and from Firestore. Passing in
`null` removes the current converter.

###### Returns

[`CollectionReference`](#collectionreference)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

A CollectionReference<U> that uses the provided converter.

###### Overrides

[`Query`](#query).[`withConverter`](#withconverter-2)

###### Call Signature

> **withConverter**\<`NewAppModelType`, `NewDbModelType`\>(`converter`): [`CollectionReference`](#collectionreference)\<`NewAppModelType`, `NewDbModelType`\>

Defined in: [types/namespaced.ts:232](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L232)

Applies a custom data converter to this CollectionReference, allowing you
to use your own custom model objects with Firestore. When you call add()
on the returned CollectionReference instance, the provided converter will
convert between Firestore data and your custom type U.

Passing in `null` as the converter parameter removes the current
converter.

###### Type Parameters

###### NewAppModelType

`NewAppModelType` *extends* [`DocumentData`](#documentdata)

###### NewDbModelType

`NewDbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### converter

[`FirestoreDataConverter`](#firestoredataconverter)\<`NewAppModelType`, `NewDbModelType`\>

Converts objects to and from Firestore. Passing in
`null` removes the current converter.

###### Returns

[`CollectionReference`](#collectionreference)\<`NewAppModelType`, `NewDbModelType`\>

A CollectionReference<U> that uses the provided converter.

###### Overrides

[`Query`](#query).[`withConverter`](#withconverter-2)

#### Properties

##### id

> **id**: `string`

Defined in: [types/namespaced.ts:158](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L158)

The collection's identifier.

##### parent

> **parent**: [`DocumentReference`](#documentreference)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\> \| `null`

Defined in: [types/namespaced.ts:164](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L164)

A reference to the containing `DocumentReference` if this is a subcollection. If this isn't a
subcollection, the reference is null.

##### path

> **path**: `string`

Defined in: [types/namespaced.ts:169](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L169)

A string representing the path of the referenced collection (relative to the root of the database).

***

### DocumentReference

Defined in: [types/namespaced.ts:307](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L307)

A `DocumentReference` refers to a document location in a Firestore database and can be used to write, read, or listen
to the location. The document at the referenced location may or may not exist. A `DocumentReference` can also be used
to create a `CollectionReference` to a subcollection.

#### Type Parameters

##### AppModelType

`AppModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

##### DbModelType

`DbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Constructors

##### Constructor

> **new DocumentReference**\<`AppModelType`, `DbModelType`\>(): [`DocumentReference`](#documentreference)\<`AppModelType`, `DbModelType`\>

###### Returns

[`DocumentReference`](#documentreference)\<`AppModelType`, `DbModelType`\>

#### Methods

##### collection()

> **collection**(`collectionPath`): [`CollectionReference`](#collectionreference)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

Defined in: [types/namespaced.ts:342](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L342)

Gets a `CollectionReference` instance that refers to the collection at the specified path.

#### Example

```js
const collectionRef = firebase.firestore().doc('users/alovelace').collection('orders');
```

###### Parameters

###### collectionPath

`string`

A slash-separated path to a collection.

###### Returns

[`CollectionReference`](#collectionreference)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

##### delete()

> **delete**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:356](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L356)

Deletes the document referred to by this DocumentReference.

#### Example

```js
await firebase.firestore().doc('users/alovelace').delete();
```

The Promise is resolved once the document has been successfully deleted from the backend
(Note that it won't resolve while you're offline).

###### Returns

`Promise`\<`void`\>

##### get()

> **get**(`options?`): `Promise`\<[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>\>

Defined in: [types/namespaced.ts:375](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L375)

Reads the document referred to by this DocumentReference.

Note: By default, get() attempts to provide up-to-date data when possible by waiting for data
from the server, but it may return cached data or fail if you are offline and the server cannot
be reached. This behavior can be altered via the GetOptions parameter.

#### Example

```js
await firebase.firestore().doc('users/alovelace').get({
  source: 'server',
});
```

###### Parameters

###### options?

[`GetOptions`](#getoptions)

An object to configure the get behavior.

###### Returns

`Promise`\<[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>\>

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:392](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L392)

Returns true if this DocumentReference is equal to the provided one.

#### Example

```js
const alovelace = firebase.firestore().doc('users/alovelace');
const dsmith = firebase.firestore().doc('users/dsmith');

// false
alovelace.isEqual(dsmith);
```

###### Parameters

###### other

[`DocumentReference`](#documentreference)\<`AppModelType`, `DbModelType`\>

The `DocumentReference` to compare against.

###### Returns

`boolean`

##### onSnapshot()

###### Call Signature

> **onSnapshot**(`observer`): () => `void`

Defined in: [types/namespaced.ts:415](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L415)

Attaches a listener for DocumentSnapshot events.

NOTE: Although an complete callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().doc('users/alovelace')
  .onSnapshot({
    error: (e) => console.error(e),
    next: (documentSnapshot) => {},
  });

unsubscribe();
```

###### Parameters

###### observer

A single object containing `next` and `error` callbacks.

###### complete?

() => `void`

###### error?

(`error`) => `void`

###### next?

(`snapshot`) => `void`

###### Returns

() => `void`

###### Call Signature

> **onSnapshot**(`options`, `observer`): () => `void`

Defined in: [types/namespaced.ts:445](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L445)

Attaches a listener for DocumentSnapshot events with snapshot listener options.

NOTE: Although an complete callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().doc('users/alovelace')
  .onSnapshot({
    includeMetadataChanges: true,
  }, {
    error: (e) => console.error(e),
    next: (documentSnapshot) => {},
  });

unsubscribe();
```

###### Parameters

###### options

[`SnapshotListenOptions`](#snapshotlistenoptions)

Options controlling the listen behavior.

###### observer

A single object containing `next` and `error` callbacks.

###### complete?

() => `void`

###### error?

(`error`) => `void`

###### next?

(`snapshot`) => `void`

###### Returns

() => `void`

###### Call Signature

> **onSnapshot**(`onNext`, `onError?`, `onCompletion?`): () => `void`

Defined in: [types/namespaced.ts:476](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L476)

Attaches a listener for DocumentSnapshot events.

NOTE: Although an onCompletion callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().doc('users/alovelace')
  .onSnapshot(
    (documentSnapshot) => {}, // onNext
    (error) => console.error(error), // onError
  );

unsubscribe();
```

###### Parameters

###### onNext

(`snapshot`) => `void`

A callback to be called every time a new `DocumentSnapshot` is available.

###### onError?

(`error`) => `void`

A callback to be called if the listen fails or is cancelled. No further callbacks will occur.

###### onCompletion?

() => `void`

An optional function which will never be called.

###### Returns

() => `void`

###### Call Signature

> **onSnapshot**(`options`, `onNext`, `onError?`, `onCompletion?`): () => `void`

Defined in: [types/namespaced.ts:506](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L506)

Attaches a listener for DocumentSnapshot events with snapshot listener options.

NOTE: Although an onCompletion callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().doc('users/alovelace')
  .onSnapshot(
    { includeMetadataChanges: true }, // SnapshotListenerOptions
    (documentSnapshot) => {}, // onNext
    (error) => console.error(error), // onError
  );

unsubscribe();
```

###### Parameters

###### options

[`SnapshotListenOptions`](#snapshotlistenoptions)

Options controlling the listen behavior.

###### onNext

(`snapshot`) => `void`

A callback to be called every time a new `DocumentSnapshot` is available.

###### onError?

(`error`) => `void`

A callback to be called if the listen fails or is cancelled. No further callbacks will occur.

###### onCompletion?

() => `void`

An optional function which will never be called.

###### Returns

() => `void`

##### set()

> **set**(`data`, `options?`): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:534](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L534)

Writes to the document referred to by this DocumentReference. If the document does not yet
exist, it will be created. If you pass SetOptions, the provided data can be merged into an
existing document.

#### Example

```js
const user = firebase.firestore().doc('users/alovelace');

// Set new data
await user.set({
  name: 'Ada Lovelace',
  age: 30,
  city: 'LON',
});
```

###### Parameters

###### data

[`SetValue`](#setvalue)\<`AppModelType`\>

A map of the fields and values for the document.

###### options?

[`SetOptions`](#setoptions)

An object to configure the set behavior.

###### Returns

`Promise`\<`void`\>

##### update()

###### Call Signature

> **update**(`data`): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:553](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L553)

Updates fields in the document referred to by this `DocumentReference`. The update will fail
if applied to a document that does not exist.

#### Example

```
const user = firebase.firestore().doc('users/alovelace');

// Update age but leave other fields untouched
await user.update({
  age: 31,
});
```

###### Parameters

###### data

`Partial`\<[`SetValue`](#setvalue)\<`DbModelType`\>\>

An object containing the fields and values with which to update the document. Fields can contain dots to reference nested fields within the document.

###### Returns

`Promise`\<`void`\>

###### Call Signature

> **update**(`field`, `value`, ...`moreFieldsAndValues`): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:572](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L572)

Updates fields in the document referred to by this DocumentReference. The update will fail if
applied to a document that does not exist.

#### Example

```
const user = firebase.firestore().doc('users/alovelace');

// Update age & city but leve other fields untouched
await user.update('age', 31, 'city', 'SF');
```

###### Parameters

###### field

[`FieldPath`](#fieldpath) \| keyof `DbModelType`

The first field to update.

###### value

`any`

The first value.

###### moreFieldsAndValues

...`any`[]

Additional key value pairs.

###### Returns

`Promise`\<`void`\>

##### withConverter()

###### Call Signature

> **withConverter**(`converter`): [`DocumentReference`](#documentreference)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

Defined in: [types/namespaced.ts:584](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L584)

Removes the current converter.

###### Parameters

###### converter

`null`

Passing in `null` removes the current converter.

###### Returns

[`DocumentReference`](#documentreference)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

A DocumentReference<U> that uses the provided converter.

###### Call Signature

> **withConverter**\<`NewAppModelType`, `NewDbModelType`\>(`converter`): [`DocumentReference`](#documentreference)\<`NewAppModelType`, `NewDbModelType`\>

Defined in: [types/namespaced.ts:596](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L596)

Applies a custom data converter to this `DocumentReference`, allowing you
to use your own custom model objects with Firestore. When you call setDoc
getDoc, etc. with the returned `DocumentReference`
instance, the provided converter will convert between Firestore data of
type `NewDbModelType` and your custom type `NewAppModelType`.

###### Type Parameters

###### NewAppModelType

`NewAppModelType` *extends* [`DocumentData`](#documentdata)

###### NewDbModelType

`NewDbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### converter

[`FirestoreDataConverter`](#firestoredataconverter)\<`NewAppModelType`, `NewDbModelType`\>

Converts objects to and from Firestore.

###### Returns

[`DocumentReference`](#documentreference)\<`NewAppModelType`, `NewDbModelType`\>

A `DocumentReference` that uses the provided converter.

#### Properties

##### firestore

> **firestore**: [`Module`](#module)

Defined in: [types/namespaced.ts:314](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L314)

The Firestore instance the document is in. This is useful for performing transactions, for example.

##### id

> **id**: `string`

Defined in: [types/namespaced.ts:319](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L319)

The document's identifier within its collection.

##### parent

> **parent**: [`CollectionReference`](#collectionreference)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:324](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L324)

The Collection this `DocumentReference` belongs to.

##### path

> **path**: `string`

Defined in: [types/namespaced.ts:329](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L329)

A string representing the path of the referenced document (relative to the root of the database).

***

### FieldPath

Defined in: [types/namespaced.ts:732](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L732)

A FieldPath refers to a field in a document. The path may consist of a single field name (referring to a
top-level field in the document), or a list of field names (referring to a nested field in the document).

Create a FieldPath by providing field names. If more than one field name is provided, the path will point to a nested field in a document.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

// Create a new field path
const fieldPath = new firebase.firestore.FieldPath('address', 'zip');

console.log('Address ZIP Code', user.get(fieldPath));
```

#### Constructors

##### Constructor

> **new FieldPath**(...`fieldNames`): [`FieldPath`](#fieldpath)

Defined in: [types/namespaced.ts:749](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L749)

Creates a FieldPath from the provided field names. If more than one field name is provided, the path will point to a nested field in a document.

#### Example

```js
const fieldPath = new firebase.firestore.FieldPath('address', line', 'one');
```

###### Parameters

###### fieldNames

...`string`[]

A list of field names.

###### Returns

[`FieldPath`](#fieldpath)

#### Methods

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:766](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L766)

Returns true if this `FieldPath` is equal to the provided one.

#### Example

```js
const fieldPath1 = new firebase.firestore.FieldPath('address', 'zip');
const fieldPath2 = new firebase.firestore.FieldPath('address', line', 'one');

// false
fieldPath1.isEqual(fieldPath2);
```

###### Parameters

###### other

[`FieldPath`](#fieldpath)

The `FieldPath` to compare against.

###### Returns

`boolean`

##### documentId()

> `static` **documentId**(): [`FieldPath`](#fieldpath)

Defined in: [types/namespaced.ts:736](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L736)

Returns a special sentinel `FieldPath` to refer to the ID of a document. It can be used in queries to sort or filter by the document ID.

###### Returns

[`FieldPath`](#fieldpath)

***

### FieldValue

Defined in: [types/namespaced.ts:782](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L782)

Sentinel values that can be used when writing document fields with `set()` or `update()`.

#### Example

```js
const increment = firebase.firestore.FieldValue.increment(1);

await firebase.firestore().doc('users/alovelace').update({
  age: increment, // increment age by 1
});
```

#### Constructors

##### Constructor

> **new FieldValue**(): [`FieldValue`](#fieldvalue)

###### Returns

[`FieldValue`](#fieldvalue)

#### Methods

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:900](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L900)

Returns true if this `FieldValue` is equal to the provided one.

#### Example

```js
const increment = firebase.firestore.FieldValue.increment(1);
const timestamp = firebase.firestore.FieldValue.serverTimestamp();

// false
increment.isEqual(timestamp);
```

###### Parameters

###### other

[`FieldValue`](#fieldvalue)

The `FieldValue` to compare against.

###### Returns

`boolean`

##### arrayRemove()

> `static` **arrayRemove**(...`elements`): [`FieldValue`](#fieldvalue)

Defined in: [types/namespaced.ts:801](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L801)

Returns a special value that can be used with `set()` or `update()` that tells the server to remove the given elements
from any array value that already exists on the server. All instances of each element specified will be removed from
the array. If the field being modified is not already an array it will be overwritten with an empty array.

#### Example

```js
const arrayRemove = firebase.firestore.FieldValue.arrayRemove(2, '3');

// Removes the values 2 & '3' from the values array on the document
await docRef.update({
  values: arrayRemove,
});
```

###### Parameters

###### elements

...`any`[]

The elements to remove from the array.

###### Returns

[`FieldValue`](#fieldvalue)

##### arrayUnion()

> `static` **arrayUnion**(...`elements`): [`FieldValue`](#fieldvalue)

Defined in: [types/namespaced.ts:822](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L822)

Returns a special value that can be used with `set()` or `update()` that tells the server to union the given
elements with any array value that already exists on the server. Each specified element that doesn't already exist
in the array will be added to the end. If the field being modified is not already an array it will be overwritten
with an array containing exactly the specified elements.

#### Example

```js
const arrayUnion = firebase.firestore.FieldValue.arrayUnion(2, '3');

// Appends the values 2 & '3' onto the values array on the document
await docRef.update({
  values: arrayUnion,
});
```

###### Parameters

###### elements

...`any`[]

The elements to union into the array.

###### Returns

[`FieldValue`](#fieldvalue)

##### delete()

> `static` **delete**(): [`FieldValue`](#fieldvalue)

Defined in: [types/namespaced.ts:838](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L838)

Returns a sentinel for use with update() to mark a field for deletion.

#### Example

```js
const delete = firebase.firestore.FieldValue.delete();

// Deletes the name field on the document
await docRef.update({
  name: delete,
});
```

###### Returns

[`FieldValue`](#fieldvalue)

##### increment()

> `static` **increment**(`n`): [`FieldValue`](#fieldvalue)

Defined in: [types/namespaced.ts:867](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L867)

Returns a special value that can be used with `set()` or `update()` that tells the server to increment the field's current value by the given value.

If either the operand or the current field value uses floating point precision, all arithmetic follows IEEE 754 semantics.
If both values are integers, values outside of JavaScript's safe number range (`Number.MIN_SAFE_INTEGER` to `Number.MAX_SAFE_INTEGER`)
are also subject to precision loss. Furthermore, once processed by the Firestore backend, all integer operations are
capped between -2^63 and 2^63-1.

If the current field value is not of type `number`, or if the field does not yet exist, the transformation sets the field to the given value.

#### Example

```js
const increment = firebase.firestore.FieldValue.increment(1);

// Increment the loginCount field by 1 on the document
await docRef.update({
  loginCount: increment,
});
```

Please be careful using this operator. It may not be reliable enough for use in circumstances where absolute accuracy is required,
as it appears writes to Firestore may sometimes be duplicated in situations not fully understood yet, but possibly correlated with
write frequency. See https://github.com/invertase/react-native-firebase/discussions/5914

###### Parameters

###### n

`number`

The value to increment by.

###### Returns

[`FieldValue`](#fieldvalue)

##### serverTimestamp()

> `static` **serverTimestamp**(): [`FieldValue`](#fieldvalue)

Defined in: [types/namespaced.ts:883](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L883)

Returns a sentinel used with set() or update() to include a server-generated timestamp in the written data.

#### Example

```js
const timestamp = firebase.firestore.FieldValue.serverTimestamp();

// Set the updatedAt field to the current server time
await docRef.update({
  updatedAt: timestamp,
});
```

###### Returns

[`FieldValue`](#fieldvalue)

***

### GeoPoint

Defined in: [types/namespaced.ts:908](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L908)

An immutable object representing a geo point in Firestore. The geo point is represented as latitude/longitude pair.

Latitude values are in the range of [-90, 90]. Longitude values are in the range of [-180, 180].

#### Constructors

##### Constructor

> **new GeoPoint**(`latitude`, `longitude`): [`GeoPoint`](#geopoint)

Defined in: [types/namespaced.ts:921](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L921)

Creates a new immutable GeoPoint object with the provided latitude and longitude values.

#### Example

```js
const geoPoint = new firebase.firestore.GeoPoint(60, -40);
```

###### Parameters

###### latitude

`number`

The latitude as number between -90 and 90.

###### longitude

`number`

The longitude as number between -180 and 180.

###### Returns

[`GeoPoint`](#geopoint)

#### Methods

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:948](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L948)

Returns true if this `GeoPoint` is equal to the provided one.

#### Example

```js
const geoPoint1 = new firebase.firestore.GeoPoint(60, -40);
const geoPoint2 = new firebase.firestore.GeoPoint(60, -20);

// false
geoPoint1.isEqual(geoPoint2);
```

###### Parameters

###### other

[`GeoPoint`](#geopoint)

The `GeoPoint` to compare against.

###### Returns

`boolean`

##### toJSON()

> **toJSON**(): `object`

Defined in: [types/namespaced.ts:953](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L953)

Returns a JSON-serializable representation of this GeoPoint.

###### Returns

`object`

###### latitude

> **latitude**: `number`

###### longitude

> **longitude**: `number`

#### Properties

##### latitude

> **latitude**: `number`

Defined in: [types/namespaced.ts:926](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L926)

The latitude of this `GeoPoint` instance.

##### longitude

> **longitude**: `number`

Defined in: [types/namespaced.ts:931](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L931)

The longitude of this `GeoPoint` instance.

***

### Module

Defined in: [types/namespaced.ts:2352](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2352)

The Firebase Cloud Firestore service is available for the default app or a given app.

#### Example: Get the firestore instance for the **default app**:

```js
const firestoreForDefaultApp = firebase.firestore();
```

#### Example: Get the firestore instance for a **secondary app**:

```js
const otherApp = firebase.app('otherApp');
const firestoreForOtherApp = firebase.firestore(otherApp);
```

#### Implements

- `_FirebaseModule`

#### Constructors

##### Constructor

> **new Module**(): [`Module`](#module)

###### Returns

[`Module`](#module)

#### Methods

##### batch()

> **batch**(): [`WriteBatch`](#writebatch)

Defined in: [types/namespaced.ts:2371](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2371)

Creates a write batch, used for performing multiple writes as a single atomic operation.
The maximum number of writes allowed in a single WriteBatch is 500, but note that each usage
of `FieldValue.serverTimestamp()`, `FieldValue.arrayUnion()`, `FieldValue.arrayRemove()`, or `FieldValue.increment()`
inside a WriteBatch counts as an additional write.

#### Example

```js
const batch = firebase.firestore().batch();
batch.delete(...);
```

###### Returns

[`WriteBatch`](#writebatch)

##### clearPersistence()

> **clearPersistence**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2515](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2515)

Aimed primarily at clearing up any data cached from running tests. Needs to be executed before any database calls
are made.

#### Example

```js
await firebase.firestore().clearPersistence();
```

###### Returns

`Promise`\<`void`\>

##### collection()

> **collection**\<`T`\>(`collectionPath`): [`CollectionReference`](#collectionreference)\<`T`\>

Defined in: [types/namespaced.ts:2384](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2384)

Gets a `CollectionReference` instance that refers to the collection at the specified path.

#### Example

```js
const collectionReference = firebase.firestore().collection('users');
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### collectionPath

`string`

A slash-separated path to a collection.

###### Returns

[`CollectionReference`](#collectionreference)\<`T`\>

##### collectionGroup()

> **collectionGroup**\<`T`\>(`collectionId`): [`Query`](#query)\<`T`\>

Defined in: [types/namespaced.ts:2400](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2400)

Creates and returns a new Query that includes all documents in the database that are contained
in a collection or subcollection with the given collectionId.

#### Example

```js
const collectionGroup = firebase.firestore().collectionGroup('orders');
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### collectionId

`string`

Identifies the collections to query over. Every collection or subcollection with this ID as the last segment of its path will be included. Cannot contain a slash.

###### Returns

[`Query`](#query)\<`T`\>

##### disableNetwork()

> **disableNetwork**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2415](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2415)

Disables network usage for this instance. It can be re-enabled via `enableNetwork()`. While the
network is disabled, any snapshot listeners or get() calls will return results from cache, and any
write operations will be queued until the network is restored.

Returns a promise that is resolved once the network has been disabled.

#### Example

```js
await firebase.firestore().disableNetwork();
```

###### Returns

`Promise`\<`void`\>

##### doc()

> **doc**\<`T`\>(`documentPath`): [`DocumentReference`](#documentreference)\<`T`, `T`\>

Defined in: [types/namespaced.ts:2428](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2428)

Gets a `DocumentReference` instance that refers to the document at the specified path.

#### Example

```js
const documentReference = firebase.firestore().doc('users/alovelace');
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### documentPath

`string`

A slash-separated path to a document.

###### Returns

[`DocumentReference`](#documentreference)\<`T`, `T`\>

##### enableNetwork()

> **enableNetwork**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2439](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2439)

Re-enables use of the network for this Firestore instance after a prior call to `disableNetwork()`.

#### Example

```js
await firebase.firestore().enableNetwork();
```

###### Returns

`Promise`\<`void`\>

##### loadBundle()

> **loadBundle**(`bundle`): `Promise`\<[`LoadBundleTaskProgress`](#loadbundletaskprogress)\>

Defined in: [types/namespaced.ts:2493](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2493)

Loads a Firestore bundle into the local cache.

#### Example

```js
const resp = await fetch('/createBundle');
const bundleString = await resp.text();
await firestore().loadBundle(bundleString);
```

###### Parameters

###### bundle

`string`

###### Returns

`Promise`\<[`LoadBundleTaskProgress`](#loadbundletaskprogress)\>

##### namedQuery()

> **namedQuery**\<`T`\>(`name`): [`Query`](#query)\<`T`\>

Defined in: [types/namespaced.ts:2504](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2504)

Reads a Firestore Query from local cache, identified by the given name.

#### Example

```js
const query = firestore().namedQuery('latest-stories-query');
const storiesSnap = await query.get({ source: 'cache' });
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### name

`string`

###### Returns

[`Query`](#query)\<`T`\>

##### persistentCacheIndexManager()

> **persistentCacheIndexManager**(): [`PersistentCacheIndexManager`](#persistentcacheindexmanager-1) \| `null`

Defined in: [types/namespaced.ts:2567](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2567)

Gets the `PersistentCacheIndexManager` instance used by this Cloud Firestore instance.
This is not the same as Cloud Firestore Indexes.
Persistent cache indexes are optional indexes that only exist within the SDK to assist in local query execution.
Returns `null` if local persistent storage is not in use.

###### Returns

[`PersistentCacheIndexManager`](#persistentcacheindexmanager-1) \| `null`

##### runTransaction()

> **runTransaction**(`updateFunction`): `Promise`\<`any`\>

Defined in: [types/namespaced.ts:2464](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2464)

Executes the given `updateFunction` and then attempts to commit the changes applied within the transaction.
If any document read within the transaction has changed, Cloud Firestore retries the `updateFunction`.
If it fails to commit after 5 attempts, the transaction fails.

The maximum number of writes allowed in a single transaction is 500, but note that each usage of
`FieldValue.serverTimestamp()`, `FieldValue.arrayUnion()`, `FieldValue.arrayRemove()`, or `FieldValue.increment()`
inside a transaction counts as an additional write.

#### Example

```js
const cityRef = firebase.firestore().doc('cities/SF');

await firebase.firestore()
  .runTransaction(async (transaction) => {
    const snapshot = await transaction.get(cityRef);
    await transaction.update(cityRef, {
      population: snapshot.data().population + 1,
    });
  });
```

###### Parameters

###### updateFunction

(`transaction`) => `Promise`\<`any`\>

###### Returns

`Promise`\<`any`\>

##### settings()

> **settings**(`settings`): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2481](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2481)

Specifies custom settings to be used to configure the Firestore instance. Must be set before invoking any other methods.

#### Example

```js
const settings = {
  cacheSizeBytes: firebase.firestore.CACHE_SIZE_UNLIMITED,
};

await firebase.firestore().settings(settings);
```

###### Parameters

###### settings

[`Settings`](#settings-1)

A `Settings` object.

###### Returns

`Promise`\<`void`\>

##### terminate()

> **terminate**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2544](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2544)

Typically called to ensure a new Firestore instance is initialized before calling
`firebase.firestore().clearPersistence()`.

#### Example

```js
await firebase.firestore().terminate();
```

###### Returns

`Promise`\<`void`\>

##### useEmulator()

> **useEmulator**(`host`, `port`): `void`

Defined in: [types/namespaced.ts:2559](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2559)

Modify this Firestore instance to communicate with the Firebase Firestore emulator.
This must be called before any other calls to Firebase Firestore to take effect.
Do not use with production credentials as emulator traffic is not encrypted.

Note: on android, hosts 'localhost' and '127.0.0.1' are automatically remapped to '10.0.2.2' (the
"host" computer IP address for android emulators) to make the standard development experience easy.
If you want to use the emulator on a real android device, you will need to specify the actual host
computer IP address.

###### Parameters

###### host

`string`

###### port

`number`

###### Returns

`void`

##### waitForPendingWrites()

> **waitForPendingWrites**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2533](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2533)

Waits until all currently pending writes for the active user have been acknowledged by the
backend.

The returned Promise resolves immediately if there are no outstanding writes. Otherwise, the
Promise waits for all previously issued writes (including those written in a previous app
session), but it does not wait for writes that were added after the method is called. If you
want to wait for additional writes, call `waitForPendingWrites()` again.

Any outstanding `waitForPendingWrites()` Promises are rejected when the logged-in user changes.

#### Example

```js
await firebase.firestore().waitForPendingWrites();
```

###### Returns

`Promise`\<`void`\>

#### Properties

##### app

> **app**: [`FirebaseApp`](../../../../@react-native-firebase/app/namespaces/ReactNativeFirebase.mdx#firebaseapp)

Defined in: [types/namespaced.ts:2356](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2356)

The current `FirebaseApp` instance for this Firebase service.

###### Implementation of

`_FirebaseModule.app`

***

### Query

Defined in: [types/namespaced.ts:1069](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1069)

A Query refers to a `Query` which you can read or listen to. You can also construct refined `Query` objects by
adding filters and ordering.

#### Extended by

- [`CollectionReference`](#collectionreference)

#### Type Parameters

##### AppModelType

`AppModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

##### DbModelType

`DbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Constructors

##### Constructor

> **new Query**\<`AppModelType`, `DbModelType`\>(): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

#### Methods

##### count()

> **count**(): [`AggregateQuery`](#aggregatequery)\<\{ `count`: [`AggregateField`](#aggregatefield)\<`number`\>; \}\>

Defined in: [types/namespaced.ts:1086](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1086)

Calculates the number of documents in the result set of the given query, without actually downloading
the documents.

Using this function to count the documents is efficient because only the final count, not the
documents' data, is downloaded. This function can even count the documents if the result set
would be prohibitively large to download entirely (e.g. thousands of documents).

The result received from the server is presented, unaltered, without considering any local state.
That is, documents in the local cache are not taken into consideration, neither are local
modifications not yet synchronized with the server. Previously-downloaded results, if any,
 are not used: every request using this source necessarily involves a round trip to the server.

###### Returns

[`AggregateQuery`](#aggregatequery)\<\{ `count`: [`AggregateField`](#aggregatefield)\<`number`\>; \}\>

##### countFromServer()

> **countFromServer**(): [`AggregateQuery`](#aggregatequery)\<\{ `count`: [`AggregateField`](#aggregatefield)\<`number`\>; \}\>

Defined in: [types/namespaced.ts:1091](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1091)

Same as count()

###### Returns

[`AggregateQuery`](#aggregatequery)\<\{ `count`: [`AggregateField`](#aggregatefield)\<`number`\>; \}\>

##### endAt()

###### Call Signature

> **endAt**(`snapshot`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1114](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1114)

Creates and returns a new Query that ends at the provided document (inclusive). The end
position is relative to the order of the query. The document must contain all of the
fields provided in the orderBy of this query.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

// Get all users up to a specific user in order of age
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .endAt(user);
```

> Cursor snapshot queries have limitations. Please see [Query limitations](/query-limitations) for more information.

###### Parameters

###### snapshot

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The snapshot of the document to end at.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Call Signature

> **endAt**(...`fieldValues`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1132](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1132)

Creates and returns a new Query that ends at the provided fields relative to the order of the query.
The order of the field values must match the order of the order by clauses of the query.

#### Example

```js
// Get all users who's age is 30 or less
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .endAt(30);
```

###### Parameters

###### fieldValues

...`any`[]

The field values to end this query at, in order of the query's order by.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

##### endBefore()

###### Call Signature

> **endBefore**(`snapshot`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1155](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1155)

Creates and returns a new Query that ends before the provided document (exclusive). The end
position is relative to the order of the query. The document must contain all of the fields
provided in the orderBy of this query.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

// Get all users up to, but not including, a specific user in order of age
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .endBefore(user);
```

> Cursor snapshot queries have limitations. Please see [Query limitations](/query-limitations) for more information.

###### Parameters

###### snapshot

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The snapshot of the document to end before.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Call Signature

> **endBefore**(...`fieldValues`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1175](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1175)

Creates and returns a new Query that ends before the provided fields relative to the order of
the query. The order of the field values must match the order of the order by clauses of the query.

#### Example

```js
// Get all users who's age is 29 or less
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .endBefore(30);
```

###### Parameters

###### fieldValues

...`any`[]

The field values to end this query before, in order of the query's order by.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

##### get()

> **get**(`options?`): `Promise`\<[`QuerySnapshot`](#querysnapshot)\<`AppModelType`, `DbModelType`\>\>

Defined in: [types/namespaced.ts:1197](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1197)

Executes the query and returns the results as a QuerySnapshot.

Note: By default, get() attempts to provide up-to-date data when possible by waiting for data from the server,
but it may return cached data or fail if you are offline and the server cannot be reached. This behavior can be
altered via the `GetOptions` parameter.

#### Example

```js
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .get({
    source: 'server',
  });
```

###### Parameters

###### options?

[`GetOptions`](#getoptions)

An object to configure the get behavior.

###### Returns

`Promise`\<[`QuerySnapshot`](#querysnapshot)\<`AppModelType`, `DbModelType`\>\>

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:1219](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1219)

Returns true if this Query is equal to the provided one.

#### Example

```js
const query = firebase.firestore()
  .collection('users')
  .orderBy('age');

// false
query.isEqual(
  firebase.firestore()
    .collection('users')
    .orderBy('name')
);
```

###### Parameters

###### other

[`Query`](#query)

The `Query` to compare against.

###### Returns

`boolean`

##### limit()

> **limit**(`limit`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1237](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1237)

Creates and returns a new Query where the results are limited to the specified number of documents.

#### Example

```js
// Get 10 users in order of age
const querySnapshot = firebase.firestore()
  .collection('users')
  .orderBy('age')
  .limit(10)
  .get();
```

###### Parameters

###### limit

`number`

The maximum number of items to return.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

##### limitToLast()

> **limitToLast**(`limitToLast`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1257](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1257)

Creates and returns a new Query where the results are limited to the specified number of documents
starting from the last document. The order is dependent on the second parameter for the `orderBy`
method. If `desc` is used, the order is reversed. `orderBy` method call is required when calling `limitToLast`.

#### Example

```js
// Get the last 10 users in reverse order of age
const querySnapshot = firebase.firestore()
  .collection('users')
  .orderBy('age', 'desc')
  .limitToLast(10)
  .get();
```

###### Parameters

###### limitToLast

`number`

The maximum number of items to return.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

##### onSnapshot()

###### Call Signature

> **onSnapshot**(`observer`): () => `void`

Defined in: [types/namespaced.ts:1280](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1280)

Attaches a listener for `QuerySnapshot` events.

> Although an `onCompletion` callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().collection('users')
  .onSnapshot({
    error: (e) => console.error(e),
    next: (querySnapshot) => {},
  });

unsubscribe();
```

###### Parameters

###### observer

A single object containing `next` and `error` callbacks.

###### complete?

() => `void`

###### error?

(`error`) => `void`

###### next?

(`snapshot`) => `void`

###### Returns

() => `void`

###### Call Signature

> **onSnapshot**(`options`, `observer`): () => `void`

Defined in: [types/namespaced.ts:1310](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1310)

Attaches a listener for `QuerySnapshot` events with snapshot listener options.

> Although an `onCompletion` callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().collection('users')
  .onSnapshot({
    includeMetadataChanges: true,
  }, {
    error: (e) => console.error(e),
    next: (querySnapshot) => {},
  });

unsubscribe();
```

###### Parameters

###### options

[`SnapshotListenOptions`](#snapshotlistenoptions)

Options controlling the listen behavior.

###### observer

A single object containing `next` and `error` callbacks.

###### complete?

() => `void`

###### error?

(`error`) => `void`

###### next?

(`snapshot`) => `void`

###### Returns

() => `void`

###### Call Signature

> **onSnapshot**(`onNext`, `onError?`, `onCompletion?`): () => `void`

Defined in: [types/namespaced.ts:1341](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1341)

Attaches a listener for `QuerySnapshot` events.

> Although an `onCompletion` callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().collection('users')
  .onSnapshot(
    (querySnapshot) => {}, // onNext
    (error) => console.error(error), // onError
  );

unsubscribe();
```

###### Parameters

###### onNext

(`snapshot`) => `void`

A callback to be called every time a new `QuerySnapshot` is available.

###### onError?

(`error`) => `void`

A callback to be called if the listen fails or is cancelled. No further callbacks will occur.

###### onCompletion?

() => `void`

An optional function which will never be called.

###### Returns

() => `void`

###### Call Signature

> **onSnapshot**(`options`, `onNext`, `onError?`, `onCompletion?`): () => `void`

Defined in: [types/namespaced.ts:1371](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1371)

Attaches a listener for `QuerySnapshot` events with snapshot listener options.

NOTE: Although an onCompletion callback can be provided, it will never be called because the snapshot stream is never-ending.

Returns an unsubscribe function to stop listening to events.

#### Example

```js
const unsubscribe = firebase.firestore().collection('users')
  .onSnapshot(
    { includeMetadataChanges: true }, // SnapshotListenerOptions
    (querySnapshot) => {}, // onNext
    (error) => console.error(error), // onError
  );

unsubscribe();
```

###### Parameters

###### options

[`SnapshotListenOptions`](#snapshotlistenoptions)

Options controlling the listen behavior.

###### onNext

(`snapshot`) => `void`

A callback to be called every time a new `QuerySnapshot` is available.

###### onError?

(`error`) => `void`

A callback to be called if the listen fails or is cancelled. No further callbacks will occur.

###### onCompletion?

() => `void`

An optional function which will never be called.

###### Returns

() => `void`

##### orderBy()

> **orderBy**(`fieldPath`, `directionStr?`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1396](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1396)

Creates and returns a new Query that's additionally sorted by the specified field, optionally in descending order instead of ascending.

* #### Example

#### Example

```js
// Get users in order of age, descending
const querySnapshot = firebase.firestore()
  .collection('users')
  .orderBy('age', 'desc')
  .get();
```

###### Parameters

###### fieldPath

`string` \| [`FieldPath`](#fieldpath)

The field to sort by. Either a string or FieldPath instance.

###### directionStr?

`"desc"` \| `"asc"`

Optional direction to sort by (`asc` or `desc`). If not specified, order will be ascending.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

##### startAfter()

###### Call Signature

> **startAfter**(`snapshot`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1423](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1423)

Creates and returns a new Query that starts after the provided document (exclusive). The start
position is relative to the order of the query. The document must contain all of the fields
provided in the orderBy of this query.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

// Get all users up to, but not including, a specific user in order of age
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .startAfter(user)
  .get();
```

> Cursor snapshot queries have limitations. Please see [Query limitations](/query-limitations) for more information.

###### Parameters

###### snapshot

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The snapshot of the document to start after.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Call Signature

> **startAfter**(...`fieldValues`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1444](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1444)

Creates and returns a new Query that starts after the provided fields relative to the order of
the query. The order of the field values must match the order of the order by clauses of the query.

#### Example

```js
// Get all users who's age is above 30
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .startAfter(30)
  .get();
```

###### Parameters

###### fieldValues

...`any`[]

The field values to start this query after, in order of the query's order by.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

##### startAt()

###### Call Signature

> **startAt**(`snapshot`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1468](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1468)

Creates and returns a new Query that starts at the provided document (inclusive). The start
position is relative to the order of the query. The document must contain all of the
fields provided in the orderBy of this query.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

// Get all users up to a specific user in order of age
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .startAt(user)
  .get();
```

> Cursor snapshot queries have limitations. Please see [Query limitations](/query-limitations) for more information.

###### Parameters

###### snapshot

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The snapshot of the document to start at.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Call Signature

> **startAt**(...`fieldValues`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1489](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1489)

Creates and returns a new Query that starts at the provided fields relative to the order of the query.
The order of the field values must match the order of the order by clauses of the query.

#### Example

```js
// Get all users who's age is 30 or above
const querySnapshot = await firebase.firestore()
  .collection('users')
  .orderBy('age')
  .startAt(30)
  .get();
```

###### Parameters

###### fieldValues

...`any`[]

The field values to start this query at, in order of the query's order by.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

##### where()

###### Call Signature

> **where**(`fieldPath`, `opStr`, `value`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1509](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1509)

Creates and returns a new Query with the additional filter that documents must contain the specified field and
the value should satisfy the relation constraint provided.

#### Example

```js
// Get all users who's age is 30 or above
const querySnapshot = await firebase.firestore()
  .collection('users')
  .where('age', '>=', 30);
  .get();
```

###### Parameters

###### fieldPath

`string` \| [`FieldPath`](#fieldpath)

The path to compare.

###### opStr

[`WhereFilterOp`](#wherefilterop)

The operation string (e.g "<", "<=", "==", ">", ">=", "!=", "array-contains", "array-contains-any", "in", "not-in").

###### value

`any`

The comparison value.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

###### Call Signature

> **where**(`filter`): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1531](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1531)

Creates and returns a new Query with the additional filter that documents must contain the specified field and
the value should satisfy the relation constraint provided.

#### Example

```js
// Get all users who's age is 30 or above
const querySnapshot = await firebase.firestore()
  .collection('users')
  .where(Filter('age', '>=', 30));
  .get();
```

###### Parameters

###### filter

[`QueryFilterConstraint`](#queryfilterconstraint)

The filter to apply to the query.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

##### withConverter()

###### Call Signature

> **withConverter**(`converter`): [`Query`](#query)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

Defined in: [types/namespaced.ts:1539](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1539)

Removes the current converter.

###### Parameters

###### converter

`null`

`null` removes the current converter.

###### Returns

[`Query`](#query)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

A Query that uses the provided converter.

###### Call Signature

> **withConverter**\<`NewAppModelType`, `NewDbModelType`\>(`converter`): [`Query`](#query)\<`NewAppModelType`, `NewDbModelType`\>

Defined in: [types/namespaced.ts:1554](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1554)

Applies a custom data converter to this Query, allowing you to use your
own custom model objects with Firestore. When you call get() on the
returned Query, the provided converter will convert between Firestore
data and your custom type U.

Passing in `null` as the converter parameter removes the current
converter.

###### Type Parameters

###### NewAppModelType

`NewAppModelType` *extends* [`DocumentData`](#documentdata)

###### NewDbModelType

`NewDbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### converter

[`FirestoreDataConverter`](#firestoredataconverter)\<`NewAppModelType`, `NewDbModelType`\>

Converts objects to and from Firestore. Passing in
`null` removes the current converter.

###### Returns

[`Query`](#query)\<`NewAppModelType`, `NewDbModelType`\>

A Query<U> that uses the provided converter.

***

### Timestamp

Defined in: [types/namespaced.ts:1804](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1804)

A Timestamp represents a point in time independent of any time zone or calendar, represented as seconds and
fractions of seconds at nanosecond resolution in UTC Epoch time.

It is encoded using the Proleptic Gregorian Calendar which extends the Gregorian calendar backwards to year one.
It is encoded assuming all minutes are 60 seconds long, i.e. leap seconds are "smeared" so that no leap second table
is needed for interpretation. Range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z.

#### Constructors

##### Constructor

> **new Timestamp**(`seconds`, `nanoseconds`): [`Timestamp`](#timestamp)

Defined in: [types/namespaced.ts:1830](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1830)

Creates a new timestamp.

###### Parameters

###### seconds

`number`

The number of seconds of UTC time since Unix epoch 1970-01-01T00:00:00Z. Must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive.

###### nanoseconds

`number`

The non-negative fractions of a second at nanosecond resolution. Negative second values with fractions must still have non-negative nanoseconds values that count forward in time. Must be from 0 to 999,999,999 inclusive.

###### Returns

[`Timestamp`](#timestamp)

#### Methods

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:1847](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1847)

Returns true if this `Timestamp` is equal to the provided one.

###### Parameters

###### other

[`Timestamp`](#timestamp)

The `Timestamp` to compare against.

###### Returns

`boolean`

##### toDate()

> **toDate**(): `Date`

Defined in: [types/namespaced.ts:1856](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1856)

Convert a Timestamp to a JavaScript Date object. This conversion causes a loss of precision since Date objects
only support millisecond precision.

Returns a JavaScript [Date](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date) with
millseconds precision.

###### Returns

`Date`

##### toJSON()

> **toJSON**(): `object`

Defined in: [types/namespaced.ts:1874](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1874)

Convert a Timestamp to a JSON object with seconds and nanoseconds members

###### Returns

`object`

###### nanoseconds

> **nanoseconds**: `number`

###### seconds

> **seconds**: `number`

##### toMillis()

> **toMillis**(): `number`

Defined in: [types/namespaced.ts:1863](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1863)

Convert a Timestamp to a numeric timestamp (in milliseconds since epoch). This operation causes a loss of precision.

The point in time corresponding to this timestamp, represented as the number of milliseconds since Unix epoch 1970-01-01T00:00:00Z.

###### Returns

`number`

##### toString()

> **toString**(): `string`

Defined in: [types/namespaced.ts:1869](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1869)

Convert a timestamp to a string in format "FirestoreTimestamp(seconds=`seconds`, nanoseconds=`nanoseconds`)",
with the `seconds` and `nanoseconds` replaced by the values in the Timestamp object

###### Returns

`string`

##### valueOf()

> **valueOf**(): `string`

Defined in: [types/namespaced.ts:1880](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1880)

Converts this object to a primitive string, which allows Timestamp objects to be compared
using the `>`, `<=`, `>=` and `>` operators.

###### Returns

`string`

##### fromDate()

> `static` **fromDate**(`date`): [`Timestamp`](#timestamp)

Defined in: [types/namespaced.ts:1810](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1810)

Creates a new timestamp from the given JavaScript [Date](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date).

###### Parameters

###### date

`Date`

The date to initialize the `Timestamp` from.

###### Returns

[`Timestamp`](#timestamp)

##### fromMillis()

> `static` **fromMillis**(`milliseconds`): [`Timestamp`](#timestamp)

Defined in: [types/namespaced.ts:1817](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1817)

Creates a new timestamp from the given number of milliseconds.

###### Parameters

###### milliseconds

`number`

Number of milliseconds since Unix epoch 1970-01-01T00:00:00Z.

###### Returns

[`Timestamp`](#timestamp)

##### now()

> `static` **now**(): [`Timestamp`](#timestamp)

Defined in: [types/namespaced.ts:1822](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1822)

Creates a new timestamp with the current date, with millisecond precision.

###### Returns

[`Timestamp`](#timestamp)

#### Properties

##### nanoseconds

> **nanoseconds**: `number`

Defined in: [types/namespaced.ts:1835](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1835)

The number of nanoseconds of this `Timestamp`;

##### seconds

> **seconds**: `number`

Defined in: [types/namespaced.ts:1840](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1840)

The number of seconds of this `Timestamp`.

## Interfaces

### AggregateQuery

Defined in: [types/namespaced.ts:1041](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1041)

The results of requesting an aggregated query.

#### Type Parameters

##### T

`T` *extends* [`AggregateSpec`](#aggregatespec)

#### Accessors

##### query

###### Get Signature

> **get** **query**(): [`Query`](#query)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

Defined in: [types/namespaced.ts:1045](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1045)

The underlying query for this instance.

###### Returns

[`Query`](#query)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

#### Methods

##### get()

> **get**(): `Promise`\<[`AggregateQuerySnapshot`](#aggregatequerysnapshot)\<`T`, [`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>\>

Defined in: [types/namespaced.ts:1062](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1062)

Executes the query and returns the results as a AggregateQuerySnapshot.

#### Example

```js
const querySnapshot = await firebase.firestore()
  .collection('users')
  .count()
  .get();
```

###### Returns

`Promise`\<[`AggregateQuerySnapshot`](#aggregatequerysnapshot)\<`T`, [`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>\>

***

### AggregateQuerySnapshot

Defined in: [types/namespaced.ts:1013](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1013)

The results of executing an aggregation query.

#### Type Parameters

##### AggregateSpecType

`AggregateSpecType` *extends* [`AggregateSpec`](#aggregatespec)

##### AppModelType

`AppModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

##### DbModelType

`DbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Accessors

##### query

###### Get Signature

> **get** **query**(): [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1022](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1022)

The underlying query over which the aggregations recorded in this
`AggregateQuerySnapshot` were performed.

###### Returns

[`Query`](#query)\<`AppModelType`, `DbModelType`\>

#### Methods

##### data()

> **data**(): [`AggregateSpecData`](#aggregatespecdata)\<`AggregateSpecType`\>

Defined in: [types/namespaced.ts:1035](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1035)

Returns the results of the aggregations performed over the underlying
query.

The keys of the returned object will be the same as those of the
`AggregateSpec` object specified to the aggregation method, and the values
will be the corresponding aggregation result.

###### Returns

[`AggregateSpecData`](#aggregatespecdata)\<`AggregateSpecType`\>

The results of the aggregations performed over the underlying
query.

***

### AggregateSpec

Defined in: [types/namespaced.ts:997](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L997)

A type whose property values are all `AggregateField` objects.

#### Indexable

> \[`field`: `string`\]: [`AggregateFieldType`](#aggregatefieldtype-1)

***

### DocumentChange

Defined in: [types/namespaced.ts:244](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L244)

A DocumentChange represents a change to the documents matching a query. It contains the document affected and the
type of change that occurred.

#### Type Parameters

##### AppModelType

`AppModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

##### DbModelType

`DbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Properties

##### doc

> **doc**: [`QueryDocumentSnapshot`](#querydocumentsnapshot)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:251](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L251)

The document affected by this change.

##### newIndex

> **newIndex**: `number`

Defined in: [types/namespaced.ts:258](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L258)

The index of the changed document in the result set immediately after this `DocumentChange`
(i.e. supposing that all prior `DocumentChange` objects and the current `DocumentChange` object have been applied).
Is -1 for 'removed' events.

##### oldIndex

> **oldIndex**: `number`

Defined in: [types/namespaced.ts:264](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L264)

The index of the changed document in the result set immediately prior to this `DocumentChange` (i.e.
supposing that all prior `DocumentChange` objects have been applied). Is -1 for 'added' events.

##### type

> **type**: [`DocumentChangeType`](#documentchangetype-1)

Defined in: [types/namespaced.ts:269](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L269)

The type of change ('added', 'modified', or 'removed').

***

### DocumentData

Defined in: [types/namespaced.ts:143](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L143)

A `DocumentData` object represents the data in a document.

#### Indexable

> \[`key`: `string`\]: `any`

***

### DocumentSnapshot

Defined in: [types/namespaced.ts:611](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L611)

A DocumentSnapshot contains data read from a document in your Firestore database. The data can be extracted with
.`data()` or `.get(:field)` to get a specific field.

For a DocumentSnapshot that points to a non-existing document, any data access will return 'undefined'.
You can use the `exists()` method to explicitly verify a document's existence.

#### Extended by

- [`QueryDocumentSnapshot`](#querydocumentsnapshot)

#### Type Parameters

##### AppModelType

`AppModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

##### DbModelType

`DbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Methods

##### data()

> **data**(): `AppModelType` \| `undefined`

Defined in: [types/namespaced.ts:646](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L646)

Retrieves all fields in the document as an Object. Returns 'undefined' if the document doesn't exist.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

console.log('User', user.data());
```

###### Returns

`AppModelType` \| `undefined`

##### exists()

> **exists**(): `boolean`

Defined in: [types/namespaced.ts:618](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L618)

Method of the `DocumentSnapshot` that signals whether or not the data exists. True if the document exists.

###### Returns

`boolean`

##### get()

> **get**\<`fieldType`\>(`fieldPath`): `fieldType`

Defined in: [types/namespaced.ts:661](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L661)

Retrieves the field specified by fieldPath. Returns undefined if the document or field doesn't exist.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

console.log('Address ZIP Code', user.get('address.zip'));
```

###### Type Parameters

###### fieldType

`fieldType` *extends* [`DocumentFieldType`](#documentfieldtype)

###### Parameters

###### fieldPath

`string` \| [`FieldPath`](#fieldpath) \| keyof `AppModelType`

The path (e.g. 'foo' or 'foo.bar') to a specific field.

###### Returns

`fieldType`

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:680](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L680)

Returns true if this `DocumentSnapshot` is equal to the provided one.

#### Example

```js
const user1 = await firebase.firestore().doc('users/alovelace').get();
const user2 = await firebase.firestore().doc('users/dsmith').get();

// false
user1.isEqual(user2);
```

###### Parameters

###### other

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The `DocumentSnapshot` to compare against.

###### Returns

`boolean`

#### Properties

##### id

> **id**: `string`

Defined in: [types/namespaced.ts:623](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L623)

Property of the `DocumentSnapshot` that provides the document's ID.

##### metadata

> **metadata**: [`SnapshotMetadata`](#snapshotmetadata)

Defined in: [types/namespaced.ts:628](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L628)

Metadata about the `DocumentSnapshot`, including information about its source and local modifications.

##### ref

> **ref**: [`DocumentReference`](#documentreference)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:633](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L633)

The `DocumentReference` for the document included in the `DocumentSnapshot`.

***

### FilterFunction()

Defined in: [types/namespaced.ts:77](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L77)

The Filter functions used to generate an instance of Filter.

#### Type Parameters

##### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

> **FilterFunction**(`fieldPath`, `operator`, `value`): [`QueryFieldFilterConstraint`](#queryfieldfilterconstraint)\<`T`\>

Defined in: [types/namespaced.ts:82](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L82)

The Filter function used to generate an instance of Filter.
e.g. Filter('name', '==', 'Ada')

#### Parameters

##### fieldPath

[`FieldPath`](#fieldpath) \| keyof `T`

##### operator

[`WhereFilterOp`](#wherefilterop)

##### value

`any`

#### Returns

[`QueryFieldFilterConstraint`](#queryfieldfilterconstraint)\<`T`\>

#### Methods

##### and()

> **and**(...`queries`): [`QueryCompositeFilterConstraint`](#querycompositefilterconstraint)\<`T`\>

Defined in: [types/namespaced.ts:96](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L96)

The Filter.and() static function used to generate a logical AND query using multiple Filter instances.
e.g. Filter.and(Filter('name', '==', 'Ada'), Filter('name', '==', 'Bob'))

###### Parameters

###### queries

...[`QueryFilterConstraint`](#queryfilterconstraint)\<`T`\>[]

###### Returns

[`QueryCompositeFilterConstraint`](#querycompositefilterconstraint)\<`T`\>

##### or()

> **or**(...`queries`): [`QueryCompositeFilterConstraint`](#querycompositefilterconstraint)\<`T`\>

Defined in: [types/namespaced.ts:91](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L91)

The Filter.or() static function used to generate a logical OR query using multiple Filter instances.
e.g. Filter.or(Filter('name', '==', 'Ada'), Filter('name', '==', 'Bob'))

###### Parameters

###### queries

...[`QueryFilterConstraint`](#queryfilterconstraint)\<`T`\>[]

###### Returns

[`QueryCompositeFilterConstraint`](#querycompositefilterconstraint)\<`T`\>

***

### FirestoreDataConverter

Defined in: [types/namespaced.ts:2309](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2309)

Converter used by `withConverter()` to transform user objects of type T
into Firestore data.

Using the converter allows you to specify generic type arguments when
storing and retrieving objects from Firestore.

#### Example

```typescript
class Post {
  constructor(readonly title: string, readonly author: string) {}

  toString(): string {
    return this.title + ', by ' + this.author;
  }
}

const postConverter = {
  toFirestore(post: Post): firebase.firestore.DocumentData {
    return {title: post.title, author: post.author};
  },
  fromFirestore(
    snapshot: firebase.firestore.QueryDocumentSnapshot,
    options: firebase.firestore.SnapshotOptions
  ): Post {
    const data = snapshot.data(options)!;
    return new Post(data.title, data.author);
  }
};

const postSnap = await firebase.firestore()
  .collection('posts')
  .withConverter(postConverter)
  .doc().get();
const post = postSnap.data();
if (post !== undefined) {
  post.title; // string
  post.toString(); // Should be defined
  post.someNonExistentProperty; // TS error
}
```

#### Type Parameters

##### AppModelType

`AppModelType`

##### DbModelType

`DbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Methods

##### fromFirestore()

> **fromFirestore**(`snapshot`, `options`): `AppModelType`

Defined in: [types/namespaced.ts:2329](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2329)

Called by the Firestore SDK to convert Firestore data into an object of
type T. You can access your data by calling: `snapshot.data(options)`.

###### Parameters

###### snapshot

[`QueryDocumentSnapshot`](#querydocumentsnapshot)\<[`DocumentData`](#documentdata), `DbModelType`\>

A QueryDocumentSnapshot containing your data and metadata.

###### options

[`SnapshotOptions`](#snapshotoptions)

The SnapshotOptions from the initial call to `data()`.

###### Returns

`AppModelType`

##### toFirestore()

###### Call Signature

> **toFirestore**(`modelObject`): [`DocumentData`](#documentdata)

Defined in: [types/namespaced.ts:2319](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2319)

Called by the Firestore SDK to convert a custom model object of type T
into a plain JavaScript object (suitable for writing directly to the
Firestore database). To use `set()` with `merge` and `mergeFields`,
`toFirestore()` must be defined with `Partial<T>`.

###### Parameters

###### modelObject

`AppModelType`

###### Returns

[`DocumentData`](#documentdata)

###### Call Signature

> **toFirestore**(`modelObject`, `options`): [`DocumentData`](#documentdata)

Defined in: [types/namespaced.ts:2320](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2320)

###### Parameters

###### modelObject

`Partial`\<`AppModelType`\>

###### options

[`SetOptions`](#setoptions)

###### Returns

[`DocumentData`](#documentdata)

***

### GetOptions

Defined in: [types/namespaced.ts:962](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L962)

An options object that configures the behavior of get() calls on DocumentReference and Query.
By providing a GetOptions object, these methods can be configured to fetch results only from the
server, only from the local cache or attempt to fetch results from the server and fall back to the
cache (which is the default).

#### Properties

##### source

> **source**: `"default"` \| `"server"` \| `"cache"`

Defined in: [types/namespaced.ts:978](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L978)

Describes whether we should get from server or cache.

Setting to `default` (or not setting at all), causes Firestore to try to retrieve an up-to-date (server-retrieved)
snapshot, but fall back to returning cached data if the server can't be reached.

Setting to `server` causes Firestore to avoid the cache, generating an error if the server cannot be reached. Note
that the cache will still be updated if the server request succeeds. Also note that latency-compensation still
takes effect, so any pending write operations will be visible in the returned data (merged into the server-provided data).

Setting to `cache` causes Firestore to immediately return a value from the cache, ignoring the server completely
(implying that the returned value may be stale with respect to the value on the server.) If there is no data in the
cache to satisfy the `get()` call, `DocumentReference.get()` will return an error and `QuerySnapshot.get()` will return an
empty `QuerySnapshot` with no documents.

***

### LoadBundleTaskProgress

Defined in: [types/namespaced.ts:2181](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2181)

Represents a progress update or a final state from loading bundles.

#### Properties

##### bytesLoaded

> **bytesLoaded**: `number`

Defined in: [types/namespaced.ts:2185](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2185)

How many bytes have been loaded.

##### documentsLoaded

> **documentsLoaded**: `number`

Defined in: [types/namespaced.ts:2190](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2190)

How many documents have been loaded.

##### taskState

> **taskState**: [`TaskState`](#taskstate-1)

Defined in: [types/namespaced.ts:2195](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2195)

Current task state.

##### totalBytes

> **totalBytes**: `number`

Defined in: [types/namespaced.ts:2200](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2200)

How many bytes are in the bundle being loaded.

##### totalDocuments

> **totalDocuments**: `number`

Defined in: [types/namespaced.ts:2205](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2205)

How many documents are in the bundle being loaded.

***

### PersistentCacheIndexManager

Defined in: [types/namespaced.ts:2151](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2151)

Returns the PersistentCache Index Manager used by the given Firestore object.
The PersistentCacheIndexManager instance, or null if local persistent storage is not in use.

#### Methods

##### deleteAllIndexes()

> **deleteAllIndexes**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2167](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2167)

Removes all persistent cache indexes. Note this function also deletes indexes
generated by `setIndexConfiguration()`, which is deprecated.

###### Returns

`Promise`\<`void`\>

##### disableIndexAutoCreation()

> **disableIndexAutoCreation**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2162](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2162)

Stops creating persistent cache indexes automatically for local query execution.
The indexes which have been created by calling `enableIndexAutoCreation()` still take effect.

###### Returns

`Promise`\<`void`\>

##### enableIndexAutoCreation()

> **enableIndexAutoCreation**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2157](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2157)

Enables the SDK to create persistent cache indexes automatically for local query
execution when the SDK believes cache indexes can help improves performance.
This feature is disabled by default.

###### Returns

`Promise`\<`void`\>

***

### QueryCompositeFilterConstraint

Defined in: [types/namespaced.ts:65](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L65)

#### Type Parameters

##### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Properties

##### operator

> **operator**: [`QueryFilterType`](#queryfiltertype)

Defined in: [types/namespaced.ts:66](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L66)

##### queries

> **queries**: [`QueryFieldFilterConstraint`](#queryfieldfilterconstraint)\<`T`\>[]

Defined in: [types/namespaced.ts:67](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L67)

***

### QueryDocumentSnapshot

Defined in: [types/namespaced.ts:690](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L690)

A QueryDocumentSnapshot contains data read from a document in your Firestore database as part of a query.
The document is guaranteed to exist and its data can be extracted with .data() or .get(:field) to get a specific field.

A QueryDocumentSnapshot offers the same API surface as a DocumentSnapshot.
Since query results contain only existing documents, the exists() method will always be true and data() will never return 'undefined'.

#### Extends

- [`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

#### Type Parameters

##### AppModelType

`AppModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

##### DbModelType

`DbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Methods

##### data()

> **data**(): `AppModelType`

Defined in: [types/namespaced.ts:712](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L712)

Retrieves all fields in the document as an Object.

#### Example

```js
const users = await firebase.firestore().collection('users').get();

for (const user of users.docs) {
  console.log('User', user.data());
}
```

###### Returns

`AppModelType`

###### Overrides

[`DocumentSnapshot`](#documentsnapshot).[`data`](#data-1)

##### exists()

> **exists**(): `true`

Defined in: [types/namespaced.ts:697](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L697)

A QueryDocumentSnapshot is always guaranteed to exist.

###### Returns

`true`

###### Overrides

[`DocumentSnapshot`](#documentsnapshot).[`exists`](#exists)

##### get()

> **get**\<`fieldType`\>(`fieldPath`): `fieldType`

Defined in: [types/namespaced.ts:661](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L661)

Retrieves the field specified by fieldPath. Returns undefined if the document or field doesn't exist.

#### Example

```js
const user = await firebase.firestore().doc('users/alovelace').get();

console.log('Address ZIP Code', user.get('address.zip'));
```

###### Type Parameters

###### fieldType

`fieldType` *extends* [`DocumentFieldType`](#documentfieldtype)

###### Parameters

###### fieldPath

`string` \| [`FieldPath`](#fieldpath) \| keyof `AppModelType`

The path (e.g. 'foo' or 'foo.bar') to a specific field.

###### Returns

`fieldType`

###### Inherited from

[`DocumentSnapshot`](#documentsnapshot).[`get`](#get-4)

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:680](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L680)

Returns true if this `DocumentSnapshot` is equal to the provided one.

#### Example

```js
const user1 = await firebase.firestore().doc('users/alovelace').get();
const user2 = await firebase.firestore().doc('users/dsmith').get();

// false
user1.isEqual(user2);
```

###### Parameters

###### other

[`DocumentSnapshot`](#documentsnapshot)\<`AppModelType`, `DbModelType`\>

The `DocumentSnapshot` to compare against.

###### Returns

`boolean`

###### Inherited from

[`DocumentSnapshot`](#documentsnapshot).[`isEqual`](#isequal-8)

#### Properties

##### id

> **id**: `string`

Defined in: [types/namespaced.ts:623](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L623)

Property of the `DocumentSnapshot` that provides the document's ID.

###### Inherited from

[`DocumentSnapshot`](#documentsnapshot).[`id`](#id-2)

##### metadata

> **metadata**: [`SnapshotMetadata`](#snapshotmetadata)

Defined in: [types/namespaced.ts:628](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L628)

Metadata about the `DocumentSnapshot`, including information about its source and local modifications.

###### Inherited from

[`DocumentSnapshot`](#documentsnapshot).[`metadata`](#metadata)

##### ref

> **ref**: [`DocumentReference`](#documentreference)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:633](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L633)

The `DocumentReference` for the document included in the `DocumentSnapshot`.

###### Inherited from

[`DocumentSnapshot`](#documentsnapshot).[`ref`](#ref)

***

### QueryFieldFilterConstraint

Defined in: [types/namespaced.ts:59](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L59)

#### Type Parameters

##### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Properties

##### fieldPath

> **fieldPath**: keyof `T` \| [`FieldPath`](#fieldpath)

Defined in: [types/namespaced.ts:60](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L60)

##### operator

> **operator**: [`WhereFilterOp`](#wherefilterop)

Defined in: [types/namespaced.ts:61](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L61)

##### value

> **value**: `any`

Defined in: [types/namespaced.ts:62](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L62)

***

### QuerySnapshot

Defined in: [types/namespaced.ts:1582](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1582)

A `QuerySnapshot` contains zero or more `QueryDocumentSnapshot` objects representing the results of a query. The documents
can be accessed as an array via the `docs` property or enumerated using the `forEach` method. The number of documents
can be determined via the `empty` and `size` properties.

#### Type Parameters

##### AppModelType

`AppModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

##### DbModelType

`DbModelType` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

#### Methods

##### docChanges()

> **docChanges**(`options?`): [`DocumentChange`](#documentchange)\<`AppModelType`, `DbModelType`\>[]

Defined in: [types/namespaced.ts:1639](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1639)

Returns an array of the documents changes since the last snapshot. If this is the first snapshot, all documents
will be in the list as added changes.

To include metadata changes, ensure that the `onSnapshot()` method includes metadata changes.

#### Example

```js
firebase.firestore().collection('users')
  .onSnapshot((querySnapshot) => {
    console.log('Metadata Changes', querySnapshot.docChanges());
  });
```

#### Example - With metadata changes

```js
firebase.firestore().collection('users')
  .onSnapshot({ includeMetadataChanges: true }, (querySnapshot) => {
    console.log('Metadata Changes', querySnapshot.docChanges({
      includeMetadataChanges: true,
    }));
  });
```

###### Parameters

###### options?

[`SnapshotListenOptions`](#snapshotlistenoptions)

`SnapshotListenOptions` that control whether metadata-only changes (i.e. only `DocumentSnapshot.metadata` changed) should trigger snapshot events.

###### Returns

[`DocumentChange`](#documentchange)\<`AppModelType`, `DbModelType`\>[]

##### forEach()

> **forEach**(`callback`, `thisArg?`): `void`

Defined in: [types/namespaced.ts:1657](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1657)

Enumerates all of the documents in the `QuerySnapshot`.

#### Example

```js
const querySnapshot = await firebase.firestore().collection('users').get();

querySnapshot.forEach((queryDocumentSnapshot) => {
  console.log('User', queryDocumentSnapshot.data());
})
```

###### Parameters

###### callback

(`result`) => `void`

A callback to be called with a `QueryDocumentSnapshot` for each document in the snapshot.

###### thisArg?

`unknown`

The `this` binding for the callback.

###### Returns

`void`

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:1679](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1679)

Returns true if this `QuerySnapshot` is equal to the provided one.

#### Example

```js
const querySnapshot1 = await firebase.firestore().collection('users').limit(5).get();
const querySnapshot2 = await firebase.firestore().collection('users').limit(10).get();

// false
querySnapshot1.isEqual(querySnapshot2);
```

> This operation can be resource intensive when dealing with large datasets.

###### Parameters

###### other

[`QuerySnapshot`](#querysnapshot)

The `QuerySnapshot` to compare against.

###### Returns

`boolean`

#### Properties

##### docs

> **docs**: [`QueryDocumentSnapshot`](#querydocumentsnapshot)\<`AppModelType`, `DbModelType`\>[]

Defined in: [types/namespaced.ts:1589](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1589)

An array of all the documents in the `QuerySnapshot`.

##### empty

> **empty**: `boolean`

Defined in: [types/namespaced.ts:1594](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1594)

True if there are no documents in the `QuerySnapshot`.

##### metadata

> **metadata**: [`SnapshotMetadata`](#snapshotmetadata)

Defined in: [types/namespaced.ts:1599](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1599)

Metadata about this snapshot, concerning its source and if it has local modifications.

##### query

> **query**: [`Query`](#query)\<`AppModelType`, `DbModelType`\>

Defined in: [types/namespaced.ts:1604](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1604)

The query on which you called get or `onSnapshot` in order to `get` this `QuerySnapshot`.

##### size

> **size**: `number`

Defined in: [types/namespaced.ts:1609](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1609)

The number of documents in the `QuerySnapshot`.

***

### SetOptions

Defined in: [types/namespaced.ts:1689](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1689)

An options object that configures the behavior of set() calls in `DocumentReference`, `WriteBatch` and `Transaction`.
These calls can be configured to perform granular merges instead of overwriting the target documents in their entirety
by providing a `SetOptions` with `merge: true`.

Using both `merge` and `mergeFields` together will throw an error.

#### Properties

##### merge?

> `optional` **merge?**: `boolean`

Defined in: [types/namespaced.ts:1694](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1694)

Changes the behavior of a `set()` call to only replace the values specified in its data argument.
Fields omitted from the `set()` call remain untouched.

##### mergeFields?

> `optional` **mergeFields?**: (`string` \| [`FieldPath`](#fieldpath))[]

Defined in: [types/namespaced.ts:1700](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1700)

Changes the behavior of `set()` calls to only replace the specified field paths.
Any field path that is not specified is ignored and remains untouched.

***

### Settings

Defined in: [types/namespaced.ts:1708](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1708)

Specifies custom configurations for your Cloud Firestore instance. You must set these before invoking any other methods.

Used with `firebase.firestore().settings()`.

#### Properties

##### cacheSizeBytes?

> `optional` **cacheSizeBytes?**: `number`

Defined in: [types/namespaced.ts:1721](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1721)

An approximate cache size threshold for the on-disk data. If the cache grows beyond this size, Firestore will start
removing data that hasn't been recently used. The size is not a guarantee that the cache will stay below that size,
only that if the cache exceeds the given size, cleanup will be attempted.

To disable garbage collection and set an unlimited cache size, use `firebase.firestore.CACHE_SIZE_UNLIMITED`.

##### host?

> `optional` **host?**: `string`

Defined in: [types/namespaced.ts:1731](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1731)

The hostname to connect to.

Note: on android, hosts 'localhost' and '127.0.0.1' are automatically remapped to '10.0.2.2' (the
"host" computer IP address for android emulators) to make the standard development experience easy.
If you want to use the emulator on a real android device, you will need to specify the actual host
computer IP address. Use of this property for emulator connection is deprecated. Use useEmulator instead

##### ignoreUndefinedProperties?

> `optional` **ignoreUndefinedProperties?**: `boolean`

Defined in: [types/namespaced.ts:1743](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1743)

Whether to skip nested properties that are set to undefined during object serialization.
If set to true, these properties are skipped and not written to Firestore.
If set to false or omitted, the SDK throws an exception when it encounters properties of type undefined.

##### persistence?

> `optional` **persistence?**: `boolean`

Defined in: [types/namespaced.ts:1712](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1712)

Enables or disables local persistent storage.

##### serverTimestampBehavior?

> `optional` **serverTimestampBehavior?**: `"estimate"` \| `"previous"` \| `"none"`

Defined in: [types/namespaced.ts:1756](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1756)

If set, controls the return value for server timestamps that have not yet been set to their final value.

By specifying 'estimate', pending server timestamps return an estimate based on the local clock.
This estimate will differ from the final value and cause these values to change once the server result becomes available.

By specifying 'previous', pending timestamps will be ignored and return their previous value instead.

If omitted or set to 'none', null will be returned by default until the server value becomes available.

##### ssl?

> `optional` **ssl?**: `boolean`

Defined in: [types/namespaced.ts:1736](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1736)

Whether to use SSL when connecting. A true value is incompatible with the firestore emulator.

***

### SnapshotListenOptions

Defined in: [types/namespaced.ts:1763](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1763)

An options object that can be passed to `DocumentReference.onSnapshot()`, `Query.onSnapshot()` and `QuerySnapshot.docChanges()`
to control which types of changes to include in the result set.

#### Properties

##### includeMetadataChanges

> **includeMetadataChanges**: `boolean`

Defined in: [types/namespaced.ts:1767](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1767)

Include a change even if only the metadata of the query or of a document changed. Default is false.

***

### SnapshotMetadata

Defined in: [types/namespaced.ts:1773](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1773)

Metadata about a snapshot, describing the state of the snapshot.

#### Methods

##### isEqual()

> **isEqual**(`other`): `boolean`

Defined in: [types/namespaced.ts:1793](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1793)

Returns true if this `SnapshotMetadata` is equal to the provided one.

###### Parameters

###### other

[`SnapshotMetadata`](#snapshotmetadata)

The `SnapshotMetadata` to compare against.

###### Returns

`boolean`

#### Properties

##### fromCache

> **fromCache**: `boolean`

Defined in: [types/namespaced.ts:1779](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1779)

True if the snapshot includes local writes (`set()` or `update()` calls) that haven't been committed to the backend yet.
If your listener has opted into metadata updates (via `SnapshotListenOptions`) you will receive another snapshot with
`fromCache` equal to false once the client has received up-to-date data from the backend.

##### hasPendingWrites

> **hasPendingWrites**: `boolean`

Defined in: [types/namespaced.ts:1786](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1786)

True if the snapshot contains the result of local writes (e.g. `set()` or `update()` calls) that have not yet been
committed to the backend. If your listener has opted into metadata updates (via `SnapshotListenOptions`) you will
receive another snapshot with `hasPendingWrites` equal to false once the writes have been committed to the backend.

***

### Statics

Defined in: [types/namespaced.ts:2211](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2211)

`firebase.firestore.X`

#### Methods

##### setLogLevel()

> **setLogLevel**(`logLevel`): `void`

Defined in: [types/namespaced.ts:2263](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2263)

Sets the verbosity of Cloud Firestore native device logs (debug, error, or silent).

- `debug`: the most verbose logging level, primarily for debugging.
- `error`: logs only error events.
- `silent`: turn off logging.

#### Example

```js
firebase.firestore.setLogLevel('silent');
```

###### Parameters

###### logLevel

`"debug"` \| `"error"` \| `"silent"`

The verbosity you set for activity and error logging.

###### Returns

`void`

#### Properties

##### Blob

> **Blob**: *typeof* [`Blob`](#blob)

Defined in: [types/namespaced.ts:2215](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2215)

Returns the `Blob` class.

##### CACHE\_SIZE\_UNLIMITED

> **CACHE\_SIZE\_UNLIMITED**: `number`

Defined in: [types/namespaced.ts:2246](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2246)

Used to set the cache size to unlimited when passing to `cacheSizeBytes` in
`firebase.firestore().settings()`.

##### FieldPath

> **FieldPath**: *typeof* [`FieldPath`](#fieldpath)

Defined in: [types/namespaced.ts:2220](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2220)

Returns the `FieldPath` class.

##### FieldValue

> **FieldValue**: *typeof* [`FieldValue`](#fieldvalue)

Defined in: [types/namespaced.ts:2225](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2225)

Returns the `FieldValue` class.

##### Filter

> **Filter**: [`FilterFunction`](#filterfunction)\<[`DocumentData`](#documentdata)\>

Defined in: [types/namespaced.ts:2240](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2240)

Returns the `Filter` function.

##### GeoPoint

> **GeoPoint**: *typeof* [`GeoPoint`](#geopoint)

Defined in: [types/namespaced.ts:2230](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2230)

Returns the `GeoPoint` class.

##### SDK\_VERSION

> **SDK\_VERSION**: `string`

Defined in: [types/namespaced.ts:2264](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2264)

##### Timestamp

> **Timestamp**: *typeof* [`Timestamp`](#timestamp)

Defined in: [types/namespaced.ts:2235](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2235)

Returns the `Timestamp` class.

***

### Transaction

Defined in: [types/namespaced.ts:1900](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1900)

A reference to a transaction. The `Transaction` object passed to a transaction's updateFunction provides the methods to
read and write data within the transaction context. See `Firestore.runTransaction()`.

A transaction consists of any number of `get()` operations followed by any number of write operations such as `set()`,
`update()`, or `delete()`. In the case of a concurrent edit, Cloud Firestore runs the entire transaction again. For example,
if a transaction reads documents and another client modifies any of those documents, Cloud Firestore retries the transaction.
This feature ensures that the transaction runs on up-to-date and consistent data.

Transactions never partially apply writes. All writes execute at the end of a successful transaction.

When using transactions, note that:
  - Read operations must come before write operations.
  - A function calling a transaction (transaction function) might run more than once if a concurrent edit affects a document that the transaction reads.
  - Transaction functions should not directly modify application state (return a value from the `updateFunction`).
  - Transactions will fail when the client is offline.

#### Methods

##### delete()

> **delete**(`documentRef`): [`Transaction`](#transaction)

Defined in: [types/namespaced.ts:1916](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1916)

Deletes the document referred to by the provided `DocumentReference`.

#### Example

```js
const docRef = firebase.firestore().doc('users/alovelace');

await firebase.firestore().runTransaction((transaction) => {
  return transaction.delete(docRef);
});
```

###### Parameters

###### documentRef

[`DocumentReference`](#documentreference)

A reference to the document to be deleted.

###### Returns

[`Transaction`](#transaction)

##### get()

> **get**\<`T`\>(`documentRef`): `Promise`\<[`DocumentSnapshot`](#documentsnapshot)\<`T`, [`DocumentData`](#documentdata)\>\>

Defined in: [types/namespaced.ts:1935](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1935)

Reads the document referenced by the provided `DocumentReference`.

#### Example

```js
const docRef = firebase.firestore().doc('users/alovelace');

await firebase.firestore().runTransaction(async (transaction) => {
  const snapshot = await transaction.get(docRef);
   // use snapshot with transaction (see set() or update())
   ...
});
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### documentRef

[`DocumentReference`](#documentreference)\<`T`\>

A reference to the document to be read.

###### Returns

`Promise`\<[`DocumentSnapshot`](#documentsnapshot)\<`T`, [`DocumentData`](#documentdata)\>\>

##### set()

> **set**\<`T`\>(`documentRef`, `data`, `options?`): [`Transaction`](#transaction)

Defined in: [types/namespaced.ts:1963](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1963)

Writes to the document referred to by the provided `DocumentReference`. If the document does not exist yet,
it will be created. If you pass `SetOptions`, the provided data can be merged into the existing document.

#### Example

```js
const docRef = firebase.firestore().doc('users/alovelace');

await firebase.firestore().runTransaction((transaction) => {
  const snapshot = await transaction.get(docRef);
  const snapshotData = snapshot.data();

  return transaction.set(docRef, {
    ...data,
    age: 30, // new field
  });
});
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### documentRef

[`DocumentReference`](#documentreference)\<`T`\>

A reference to the document to be set.

###### data

`T`

An object of the fields and values for the document.

###### options?

[`SetOptions`](#setoptions)

An object to configure the set behavior.

###### Returns

[`Transaction`](#transaction)

##### update()

###### Call Signature

> **update**\<`T`\>(`documentRef`, `data`): [`Transaction`](#transaction)

Defined in: [types/namespaced.ts:1990](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1990)

Updates fields in the document referred to by the provided `DocumentReference`. The update will fail if applied
to a document that does not exist.

#### Example

```js
const docRef = firebase.firestore().doc('users/alovelace');

await firebase.firestore().runTransaction((transaction) => {
  const snapshot = await transaction.get(docRef);

  return transaction.update(docRef, {
    age: snapshot.data().age + 1,
  });
});
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### documentRef

[`DocumentReference`](#documentreference)\<`T`\>

A reference to the document to be updated.

###### data

`Partial`\<\{ \[K in keyof T\]: T\[K\] \| FieldValue \}\>

An object containing the fields and values with which to update the document. Fields can contain dots to reference nested fields within the document.

###### Returns

[`Transaction`](#transaction)

###### Call Signature

> **update**\<`T`, `K`\>(`documentRef`, `field`, `value`, ...`moreFieldsAndValues`): [`Transaction`](#transaction)

Defined in: [types/namespaced.ts:2018](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2018)

Updates fields in the document referred to by the provided DocumentReference. The update will fail if applied to
a document that does not exist.

Nested fields can be updated by providing dot-separated field path strings or by providing FieldPath objects.

#### Example

```js
const docRef = firebase.firestore().doc('users/alovelace');

await firebase.firestore().runTransaction((transaction) => {
  const snapshot = await transaction.get(docRef);

  return transaction.update(docRef, 'age', snapshot.data().age + 1);
});
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### K

`K` *extends* `string` \| `number` \| `symbol` = `string`

###### Parameters

###### documentRef

[`DocumentReference`](#documentreference)\<`T`\>

A reference to the document to be updated.

###### field

[`FieldPath`](#fieldpath) \| `K`

The first field to update.

###### value

`T`\[`K`\]

The first value.

###### moreFieldsAndValues

...`any`[]

Additional key/value pairs.

###### Returns

[`Transaction`](#transaction)

***

### WriteBatch

Defined in: [types/namespaced.ts:2036](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2036)

A write batch, used to perform multiple writes as a single atomic unit.

A WriteBatch object can be acquired by calling `firestore.batch()`. It provides methods for adding
writes to the write batch. None of the writes will be committed (or visible locally) until
`WriteBatch.commit()` is called.

Unlike transactions, write batches are persisted offline and therefore are preferable when you don't need to
condition your writes on read data.

#### Methods

##### commit()

> **commit**(): `Promise`\<`void`\>

Defined in: [types/namespaced.ts:2053](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2053)

Commits all of the writes in this write batch as a single atomic unit.

Returns a Promise resolved once all of the writes in the batch have been successfully written
to the backend as an atomic unit. Note that it won't resolve while you're offline.

#### Example

```js
const batch = firebase.firestore().batch();

// Perform batch operations...

await batch.commit();
```

###### Returns

`Promise`\<`void`\>

##### delete()

> **delete**(`documentRef`): [`WriteBatch`](#writebatch)

Defined in: [types/namespaced.ts:2069](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2069)

Deletes the document referred to by the provided `DocumentReference`.

#### Example

```js
const batch = firebase.firestore().batch();
const docRef = firebase.firestore().doc('users/alovelace');

batch.delete(docRef);
```

###### Parameters

###### documentRef

[`DocumentReference`](#documentreference)

A reference to the document to be deleted.

###### Returns

[`WriteBatch`](#writebatch)

##### set()

> **set**\<`T`\>(`documentRef`, `data`, `options?`): [`WriteBatch`](#writebatch)

Defined in: [types/namespaced.ts:2092](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2092)

Writes to the document referred to by the provided DocumentReference. If the document does
not exist yet, it will be created. If you pass SetOptions, the provided data can be merged
into the existing document.

#### Example

```js
const batch = firebase.firestore().batch();
const docRef = firebase.firestore().doc('users/dsmith');

batch.set(docRef, {
  name: 'David Smith',
  age: 25,
});
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### documentRef

[`DocumentReference`](#documentreference)\<`T`\>

A reference to the document to be set.

###### data

`T`

An object of the fields and values for the document.

###### options?

[`SetOptions`](#setoptions)

An object to configure the set behavior.

###### Returns

[`WriteBatch`](#writebatch)

##### update()

###### Call Signature

> **update**\<`T`\>(`documentRef`, `data`): [`WriteBatch`](#writebatch)

Defined in: [types/namespaced.ts:2115](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2115)

Updates fields in the document referred to by the provided DocumentReference. The update will fail if applied to a document that does not exist.

#### Example

```js
const batch = firebase.firestore().batch();
const docRef = firebase.firestore().doc('users/alovelace');

batch.update(docRef, {
  city: 'SF',
});
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### Parameters

###### documentRef

[`DocumentReference`](#documentreference)\<`T`\>

A reference to the document to be updated.

###### data

`Partial`\<\{ \[K in keyof T\]: T\[K\] \| FieldValue \}\>

An object containing the fields and values with which to update the document. Fields can contain dots to reference nested fields within the document.

###### Returns

[`WriteBatch`](#writebatch)

###### Call Signature

> **update**\<`T`, `K`\>(`documentRef`, `field`, `value`, ...`moreFieldAndValues`): [`WriteBatch`](#writebatch)

Defined in: [types/namespaced.ts:2139](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2139)

Updates fields in the document referred to by this DocumentReference. The update will fail if applied to a document that does not exist.

Nested fields can be update by providing dot-separated field path strings or by providing FieldPath objects.

#### Example

```js
const batch = firebase.firestore().batch();
const docRef = firebase.firestore().doc('users/alovelace');

batch.update(docRef, 'city', 'SF', 'age', 31);
```

###### Type Parameters

###### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

###### K

`K` *extends* `string` \| `number` \| `symbol` = `string`

###### Parameters

###### documentRef

[`DocumentReference`](#documentreference)\<`T`\>

A reference to the document to be updated.

###### field

[`FieldPath`](#fieldpath) \| `K`

The first field to update.

###### value

[`FieldValue`](#fieldvalue) \| `T`\[`K`\]

The first value.

###### moreFieldAndValues

...`any`[]

Additional key value pairs.

###### Returns

[`WriteBatch`](#writebatch)

## Type Aliases

### AggregateFieldType

> **AggregateFieldType** = [`AggregateField`](#aggregatefield)\<`number`\>

Defined in: [types/namespaced.ts:992](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L992)

The union of all `AggregateField` types that are supported by Firestore.

***

### AggregateSpecData

> **AggregateSpecData**\<`T`\> = `{ [P in keyof T]: T[P] extends AggregateField<infer U> ? U : never }`

Defined in: [types/namespaced.ts:1006](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1006)

A type whose keys are taken from an `AggregateSpec`, and whose values are the
result of the aggregation performed by the corresponding `AggregateField`
from the input `AggregateSpec`.

#### Type Parameters

##### T

`T` *extends* [`AggregateSpec`](#aggregatespec)

***

### DocumentChangeType

> **DocumentChangeType** = `"added"` \| `"removed"` \| `"modified"`

Defined in: [types/namespaced.ts:275](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L275)

The type of a DocumentChange may be 'added', 'removed', or 'modified'.

***

### DocumentFieldType

> **DocumentFieldType** = `string` \| `number` \| `boolean` \| \{\[`key`: `string`\]: [`DocumentFieldType`](#documentfieldtype); \} \| [`DocumentFieldType`](#documentfieldtype)[] \| `null` \| [`Timestamp`](#timestamp) \| [`GeoPoint`](#geopoint) \| [`Blob`](#blob) \| [`FieldPath`](#fieldpath) \| [`FieldValue`](#fieldvalue) \| [`DocumentReference`](#documentreference)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\> \| [`CollectionReference`](#collectionreference)\<[`DocumentData`](#documentdata), [`DocumentData`](#documentdata)\>

Defined in: [types/namespaced.ts:287](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L287)

The types for a DocumentSnapshot field that are supported by Firestore.

***

### QueryFilterConstraint

> **QueryFilterConstraint**\<`T`\> = [`QueryFieldFilterConstraint`](#queryfieldfilterconstraint)\<`T`\> \| [`QueryCompositeFilterConstraint`](#querycompositefilterconstraint)\<`T`\>

Defined in: [types/namespaced.ts:70](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L70)

#### Type Parameters

##### T

`T` *extends* [`DocumentData`](#documentdata) = [`DocumentData`](#documentdata)

***

### QueryFilterType

> **QueryFilterType** = `"OR"` \| `"AND"`

Defined in: [types/namespaced.ts:57](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L57)

An instance of Filter used to generate Firestore Filter queries.

***

### SetValue

> **SetValue**\<`T`\> = `T` *extends* [`Timestamp`](#timestamp) ? [`Timestamp`](#timestamp) \| `Date` : `T` *extends* `object` ? \{ \[P in keyof T\]: SetValue\<T\[P\]\> \| FieldValue \} : `T`

Defined in: [types/namespaced.ts:2573](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2573)

Utility type to allow FieldValue and to allow Date in place of Timestamp objects.

#### Type Parameters

##### T

`T`

***

### SnapshotOptions

> **SnapshotOptions** = `object`

Defined in: [types/namespaced.ts:280](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L280)

An options object that configures how document snapshot data is returned (e.g. server timestamps).

#### Properties

##### serverTimestamps?

> `readonly` `optional` **serverTimestamps?**: `"estimate"` \| `"previous"` \| `"none"`

Defined in: [types/namespaced.ts:281](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L281)

***

### TaskState

> **TaskState** = `"Error"` \| `"Running"` \| `"Success"`

Defined in: [types/namespaced.ts:2176](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L2176)

Represents the state of bundle loading tasks.

Both 'Error' and 'Success' are sinking state: task will abort or complete and there will be no more
updates after they are reported.

***

### WhereFilterOp

> **WhereFilterOp** = `"<"` \| `"<="` \| `"=="` \| `">"` \| `">="` \| `"!="` \| `"array-contains"` \| `"array-contains-any"` \| `"in"` \| `"not-in"`

Defined in: [types/namespaced.ts:1565](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L1565)

Filter conditions in a `Query.where()` clause are specified using the strings '<', '<=', '==', '>=', '>', 'array-contains', 'array-contains-any' or 'in'.

## Variables

### Filter

> `const` **Filter**: [`FilterFunction`](#filterfunction)

Defined in: [types/namespaced.ts:102](https://github.com/invertase/react-native-firebase/blob/main/packages/firestore/lib/types/namespaced.ts#L102)

The Filter function used to generate an instance of Filter.
e.g. Filter('name', '==', 'Ada')
